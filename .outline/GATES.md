# Gates: bamTiScript stable TypeScript 7.0.2 completion

Scope: Complete the clean-room Rust compiler, TypeScript-compatible Node 24 API package, ECMAScript runtime, native tiers, formal proof bindings, target cells, and release evidence for the stable TypeScript 7.0.2 product contract.

- [ ] G1: Every leaf and integration ledger is met with recorded evidence
  CHECK: python3 /home/alpha/.claude/plugins/cache/odin-marketplace/odin/1.17.108/skills/unlazy/scripts/gate_check.py --status .outline/gates/*.md
  EXPECT: ALL MET
  EVIDENCE: 2026-08-28 not run to ALL MET; leaf ledgers still have unchecked G1-G6 boxes (F2.1, A3.1, C4.2, A2.7 G2-G6)

- [ ] G2: The immutable authority and generated catalogs bind stable TypeScript 7.0.2 and no stale primary TypeScript release
  CHECK: cargo run -p bamts-verification -- completion verify --root authority
  EXPECT: AUTHORITY COMPLETE release=typescript-7.0.2
  EVIDENCE: 2026-08-28 `bamts-verification completion verify --root authority` exit 1: unproven children A1.1 through A1.5, A2.1 through A2.3, A2.6, A2.7, and 11 more

- [ ] G3: Every applicable stable TypeScript 7.0.2 compiler, toolchain, CLI, and historical-compatibility obligation has an observed PASS receipt
  CHECK: cargo run -p bamts-verification -- completion verify --root compiler
  EXPECT: COMPILER COMPLETE release=typescript-7.0.2 blocking=0 external_blocked=0
  EVIDENCE: 2026-08-28 `completion verify --root compiler` exit 1: unproven B0.1, B1.1 through B1.4, B2.1, B2.2, B3.1 through B3.3, and 19515 more. Ledger n=78498 PASS=0 BLOCKING_FAIL=71750
  BASELINE: 2026-09-02 `bamts-verification suite run --catalog typescript-7.0.2 --shard i/64` with `BAMTS_SUITE_COMPILER_ADAPTER` set, receipts under `target/evidence-sweep/typescript-7.0.2/`. This is a shard sweep, not the CHECK above. 66786 rows: PASS 8951 (13.4%), BLOCKING_FAIL 51141 (76.6%), INAPPLICABLE_LANGUAGE_SERVICE 6693, TIMEOUT 1. Per-facet PASS: diagnostics 6525/15484, symbols 1846/14621, source-map 8/237, types 440/15918, declaration 61/2215, javascript 71/14096, parse 0/3718, trace 0/487, build-info 0/10. The 2026-08-28 line above reads PASS=0 because no adapter was registered, so every cell recorded `no registered adapter` and nothing was measured. These receipts pre-date 979b78c and 6d1ecc3, so they are a baseline for ranking work, never publishable completion evidence.
  FACETS: 2026-09-02 the per-facet figures above are `PASS/total`, which reads every non-PASS row as outstanding work and is wrong for any facet carrying inapplicable rows. Cross-tabulating the same receipts by facet and state gives the applicable denominator, `PASS + BLOCKING_FAIL`. javascript 71/14060 pass, 13989 blocking, 36 inapplicable. types 440/13950, 13510 blocking, 1967 inapplicable. symbols 1846/13951, 12105 blocking, 670 inapplicable. diagnostics 6525/15182, 8657 blocking, 302 inapplicable. declaration 61/2215, 2154 blocking. trace 0/487, 487 blocking. source-map 8/237, 229 blocking. build-info 0/10, 10 blocking. parse has zero applicable rows: all 3718 are fourslash language-service cases and all are INAPPLICABLE_LANGUAGE_SERVICE, so `parse 0/3718` names an exclusion, never a gap. Pass share of applicable rows is 8951/60092 = 14.9%; the 13.4% above divides by all 66786 rows including exclusions.
  RANKING: 2026-09-02 blocking mass by facet, which is the work order: javascript 13989, types 13510, symbols 12105, diagnostics 8657, declaration 2154, trace 487, source-map 229, build-info 10. Two facets have zero passing applicable rows: trace, whose largest single family is 78 rows of `classification/execution drift: no owned .trace.json baseline` and 31 of `invalid project trace fixture: missing field inputFiles`, and build-info, which has no observer function at all. Classification drift is not systemic: 218 of 51141 blocking rows (0.4%) carry it, over missing `.js` (127), `.trace.json` (78), `.js.map` (9), `.types` (2), and `.symbols` (2) baselines, so the blocking count stands essentially as measured.
  JAVASCRIPT: 2026-09-02 12753 of 13989 javascript blocking rows (91.2%) carry one opaque detail, `javascript structural mismatch after formatting/order normalization`, which names no delta. Compiling 112 of them at HEAD and diffing against the reference `.js` classifies them as 72 reshape, 24 dropped-lines, 3 changed-lines, 12 emitting no `.js`, 1 identical, and every sampled family's first delta is the same missing `"use strict";` prologue. Extraction caveat for whoever re-measures: a reference baseline's last `//// [name]` section is often the `.d.ts`, so the section whose name ends in `.js` must be selected by name; taking the last section silently compares against declaration text and contaminated an earlier 86% figure.
  PROLOGUE: 2026-09-02 measured over 10781 single-file baselines holding a real `.js` section. A file that is not an external module carries the prologue in 8644 cases and omits it in 82 (99.1%). An external module carries it in 1241 and omits it in 814, and the carriers concentrate in the script-format module kinds: `commonjs` 408 and a defaulted `module` 730. `crates/bamts-compiler/src/emitter/transforms.rs` gates emission on `always_strict || module_kind_implies_strict`, so it agrees on external modules and diverges on non-modules whenever `alwaysStrict` resolves false: 1437 of the 1597 baselines declaring `@strict: false` carry the prologue that gate omits. That is the single largest grounded javascript conversion available and it is not yet implemented.
  JAVASCRIPT: 2026-09-03 v2 sweep (2048/2048 shards, lane binary on f8c28dc with the prologue implementation): javascript PASS 2015/14096 (14.3%), BLOCKING_FAIL 12045, up from 71/14060 on 2026-09-02. The +1944 spans the whole emitter campaign in range (prologue 93cc1eb/4756929 plus parens print 67790b5, declare/export align f36bbbc, the sourcemap series, jsdoc c51f49e, modern-js lowering ef67a7d), so it is not prologue-isolated.
  PROLOGUE: 2026-09-03 residual sample at release binary 6cd8e0e (seed 20260903, N=300 of 12045 BLOCKING_FAIL javascript rows, row options from each receipt configuration plus harness strict/alwaysStrict defaults): 207 compared with 0 prologue deltas (95% upper bound about 1.4%); skips are multi-unit 69, no-reference 14, outfile-config 2, ambiguous-reference 1, missing .js section 2; 5 no-emit CLI gaps on exotic configs (system/isolatedModules/jsx, silent rc=1). Committed prologue unit tests pass 6/6. The prologue rule is done at HEAD within the comparable population; the residual is other families.
  RESIDUAL: 2026-09-03 layered whole-document census at release binary (seed 600613, N=400 of 12045 BLOCKING_FAIL javascript rows; replicator calibrated EQUAL on 57 comparable PASS rows of 60 sampled (2 declaration-config + 1 multi-unit skipped), validating pairing, echo-strip, and framing assembly on those rows): WHITESPACE_ONLY 54, FIELD_INIT 29, COMMENT 27, DECORATOR 14, EXPORT_ELISION 2, OTHER_CODE 133, WOULD_PASS 5, COMPILE_FAIL 3; skips multi-unit 50, declaration-config 53, non-ts-source 25, outfile-config 5. The 5 WOULD_PASS rows (replication passes, sweep fails; 4 with sourcemap=true) are unexplained divergences with a worker-vs-CLI audit queued, so CLI-vs-worker equivalence is NOT established. No framing-only rows exist because the comparator's blank-sensitive document compare surfaces framing blanks as content diffs. Work order by mass: brace/paren whitespace shape, spurious field-init synthesis (`this.x = void 0`), comment drops, legacy decorator strategy.
  FIELD_INIT: 2026-09-03 unconditional void-0 synthesis dropped under Assign semantics (b146af3; transforms.rs leaf-owned, no window needed; the CLI emit mapping takes 9 fixed keys with no useDefineForClassFields entry, so all knob runs defaulted and the construction default was unconditionally mode-blind): census bucket 29/400 to 8/400, remainder temp-naming and module-transform artifacts; headline baseline computedPropertyNames36_ES5 verified at CLI; suite 1801/0, clippy clean. Worker-side mapping under scout investigation; if both paths default the pin, the 75+110 pinned rows are an option-plumbing slice, not codegen.
  TRIVIA: 2026-09-03 six whitespace exhibits confirm upstream preserves authored line structure in both directions (single-line stays single-line, multi-line stays multi-line) while bamts normalizes empties to `{}` and expands the rest; empty declaration bodies excepted (`export class Object {}` expands to two lines). Mechanism decision (reprint-with-trivia vs per-construct rules) pending red-team given source-map column blast radius over 2015 passing rows.
  CLASS-EXPAND: 2026-09-03 class half of the trivia split resolved by strict doc-header-aware census (4501 single-unit `.ts` docs; full 17047-section enumeration with every tight line classified by case-source reading): zero single-line classes in any true `.js` output. No verbatim passthrough exists — even untransformed JS outputs reprint (`{}` to `{ }`, `{a}` to `{ a }`); every `{}` keeper is an echo, declaration-only, error, or comment artifact (definitive instrument leaves REVIEW bucket empty). Fix in `emit_class_body_js` (the `else newline` the declaration printer already had) plus byte-exact unit test; exhibit-1 CLI-verified converting. Suites 1804/0 and 598/0, clippy/fmt clean. Row conversions measured at next sweep, not claimed (315 `.ts` cases hold `class X {}`). Untouched with masses: empty objects keep `{}` (438 tight vs 0 spaced, `emit_object` correct); empty enums unobserved TS-side; es5/es3 class+sync-arrow downlevel absent by current error+preserve design (1334 js rows, needs its own lowering slice); amd/umd/system transforms unimplemented (583 js rows); conflict-marker scanner gap (6 js rows); empty `switch` unobserved either way (site untouched).
  BLOCK-SPACE: 2026-09-03 same instrument proves empty blocks normalize to `{ }`: 2011 spaced paren-blocks plus 61 arrows/try/static and 243 constructors vs zero true-tight outputs; normalization proven tight-source to spaced-output (`expandoFunctionSymbolProperty` `function inner() {}` to `{ }`). Fix in `emit_block_with_braces` (synthesized unmapped space) and `emit_constructor_js` (second `raw("{}")` site found by its own failing decorator test), plus shape test, mapping-geometry update (close 16 to 17, EOL 17 to 18), and decorator-expectation updates. Suites 1804/0 and 598/0, clippy/fmt clean; CLI verified (expando inner, empty ctor/method). Non-empty single-line bodies are a different rule — PRESERVED, not expanded (292 fn + 579 obj + 3 arrow true outputs): follow-up architecture slice (trivia preservation), not a mirror fix.
  ADVISORIES-2026-09-03: devin-blocker on unconditional class expansion REJECTED with the airtight evidence above (cited keepers are echoes: Test.js is declaration-only, node16 esm keeper sits in the echo section; transformed and untransformed JS outputs both expand). Default-advisory expectation of zero single-line functions FALSIFIED (292+579+3 preserved); its census-receipt proposal ADOPTED and it supersedes red-team for the class+block slices. qwen binding check ADOPTED and PASSED (2048 shards, four digests each count 2048, rows 66786 equal the catalog total). qwen digest paradox CONFIRMED by code reading (canonical homes `verification/evidence/<release>` and `proof/completeness-ledger.json` are tracked; merge at completion.rs:972 and rebuild at rebuild.rs:401 demand binding equal HEAD tree; dirty-check rejects untracked): publishing a receipt set or ledger invalidates its own binding by construction, so the standing `ledger rebuild --check` E_SET_MISMATCH is partly structural (remainder is coverage: most obligations have no receipt). The GENERATED_BINDINGS exclusion fix is a verification-architecture change, QUEUED not implemented. qwen plumbing pair STALE: every cited item already landed (project.rs tri-state field/parse/test, program.rs carry, pipeline forward, apply_emit_fields Option, both path forwards); the 1558 decorator/helper rows were already recorded as separate modeling slices.
  CTOR-CLOSE: 2026-09-03 non-empty constructor bodies closed with unmapped `raw("}")` while the empty fast path mapped its close; every ES2015 class with a non-empty constructor emitted the short map against the documented every-close-gets-a-segment invariant. One-line fix (`raw_mapped_char_end`), own commit 3c899ec, exact-segment suites referee: 1804/0 and 598/0, clippy/fmt clean.
  INTERFACE: 2026-09-03 empty interfaces split by source shape in true declaration outputs (24 tight vs 13 expanded): tight-authored stays tight (`exportAssignedNamespaceIsVisibleInDeclarationEmit` `Bar {}`), multi-line-authored stays expanded (`commentsInterface` `i1 {`). The advisor's zero-either-way pass was wrong on both counts; the existing tight test pin STANDS with an authority citation added. General rule folds into the preservation slice, not a printer fix.
  PARADOX-REVISION: 2026-09-03 the fixed-point wording is retracted to stale-by-design. Ignored shard paths never enter the porcelain check, so commit, sweep, and merge at one clean HEAD is paradox-free; staleness on the next commit is the currency design, and f8c28dc shards must not merge at HEAD (completion.rs:973 rejects the binding). What stands: publishing a tracked merged receipt or ledger means the next `--check` needs a same-HEAD re-sweep, and the standing E_SET_MISMATCH remains chiefly coverage (most obligations receiptless). Per-slice targeted recounts keep attribution that batched sweeps lose.
  PRESERVATION-CELLS: 2026-09-03 open cells closed by measurement. Catch blocks space (`catch: 15` in the keyword buckets). Multi-line-authored objects show no collapse (177 sources screened, 8 single-line outputs all pair-matched to single-line sources, 2 verified byte-paired). Source-side singles undercount outputs (fn 115 vs 292, obj 220 vs 579) because outputs add synthesized forms (helpers, IIFEs, accessors) — consistent with 1:1 preservation plus synthesis, not reflow. The ES5 synthesized-constructor memory citation is retracted as unverified (no such form in the corpus; the lowering slice pins from data). Design for next slice: source-range newline check per non-empty body/object construct; classes stay always-expand, empty blocks always-space, empty objects always-tight.
  PLUMBING: 2026-09-03 scout-mapped end to end: useDefineForClassFields is dropped on BOTH paths (CLI: no CompilerOptions field, project.rs:918; worker: build_tsconfig emits lowercase string-quoted key the case-sensitive lookup misses, plus no field), so both default to target and agree by coincidence — census valid, no contamination. Fix: Option<bool> field + parse arm + accessor (project.rs), new apply_emit_fields param set-when-Some (emitter.rs), pass-through (effective.rs, pipeline.rs), ResolvedProgram carry (program.rs, shared root, window required), build_tsconfig bool list + remap (check_cells.rs). 75 pinned-true rows below es2022 plus 110 pinned-false at es2022+ ride this one fix. alwaysStrict worker-pins-true vs CLI-derives-from-strict divergence noted, out of slice.
  PLUMBING-RESULTS: 2026-09-03 fix landed c17ab5e (window acquired with delivered receipt, inbox empty throughout, released with pre/post hashes; suites compiler 1802/0 verification 598/0, clippy and fmt clean): camelCase pin produces defineProperty emit at CLI while lowercase keys sit in raw and are silently ignored (tsc errors TS5023; unknown-key tolerance is by design per project.rs:915, case gap noted). 88-row divergent set pre/post diff: 1 full conversion (override18 FIELD_INIT to satisfied), 4 mode-advanced with the next real delta exposed (2 comment, 1 decorator, 1 temp-naming), 2 relabeled by ref-side token noise in the bucket rule (zero void-0 in either emit), 62 unchanged (pin-irrelevant code or agreed defaults). Sweep effect pending worker rebuild. Follow-ups, same mechanism: import_helpers field exists but unplumbed on both paths (877 helper-policy rows). Separate modeling slices: experimentalDecorators/emitDecoratorMetadata (516/119 rows, no fields), downlevelIteration (46), isolatedModules (82).
  SWEEP-FRESH: 2026-09-04 full 2048-shard resweep at 89b0455 with the pre-flighted HEAD worker (repo target rebuilt without the CARGO_TARGET_DIR override; header binary digest asserted equal before launch; Node 24.18.0; run sweep-89b0455-fresh): acceptance 2048 headers uniform on all four digests, 66786 rows, 0 logged SHARD_FAIL lines; 130 stranded tmp orphans accumulated across the three driver launches (cleaned post-acceptance). PASS 12354 to 13127 (+773), BLOCKING_FAIL down equally, 0 regressions, all flips in javascript (2015 to 2788, +38%). Exhibit-1 es2015 row flips FAIL to PASS at worker level (class rule causally pinned); its es5 twin stays failing by the recorded downlevel scope. Mechanism cuts are co-occurrence, batch-measured together, not isolated: tight-empty sources 23 class-pattern + 61 block-pattern (disjoint), 8 explicitly pinned rows, spaced/multi-line-authored empties dominate the remainder with field-init Assign rows, 47 residual without an isolated pattern (possibly export-synthesis side effects of the field-init guard). The first sweep-89b0455 run measured a stale 16:28 worker (zero flips, correctly discarded, never claimed).
  INTERFACE-REVISION: 2026-09-04 the 89b0455 INTERFACE line is superseded: true declaration outputs ALWAYS expand empty interfaces (0 tight / 21 expand; importTag16 a.d.ts expands tight-authored `export interface I {}`). All 24 tights traced to d.ts input copies (Bar thing.d.ts echo sole occurrence; ISpinButton cross-dir stem collision). Fix is a caller-side pre-check in `emit_interface_decl` (expand + return); shared `emit_type_members_block` stays tight because empty OBJECT TYPES stay tight (mappedTypes1 `x1: {}`, es6Import `_default: {}` transformation-proven, 0 expands observed in the true-output census). Test flipped to expanded with the F415-byte citation (the 3c899ec preservation citation was wrong). Suites 1804/0 and 598/0, clippy/fmt clean.
  PRESERVATION-BLOCK: 2026-09-04 block-body gate landed (8a3d98c): non-empty bodies authored without a line break keep `{ stmt; stmt; }` (292-function + 3-arrow families); synthesized, past-authored-text, or multi-line-authored bodies expand. Machinery: `spans_multiple_lines` (source.rs), rewriter taint set with child-mark climb (transforms.rs), `preserves_single_line` with taint/length/mint gates (emitter.rs). Window variant dropped (no consumer after the red-pen cut). Suites 1814/0 and 598/0, clippy/fmt clean. Map test restored to its multi-line authority fixture plus a new single-line method map test (hand-decoded golden: greet(4) return(14) 1(21) ;(22) }(24) EOL(25), class-close EOL(1) unchanged). CLI spots: single-line fixture emits `function f() { return 1; }`; expandoFunctionSymbolProperty emits `function inner() { }` (spaced guard holds end to end); pre/post-gate outputs byte-identical on both authority files. Limitation: mappedTypes1's x1 resolves to `any` in our checker, so no CLI run exercised the `{}`-tight object path (pinned by unit suite only); tsc bytes are `declare let x1: {};` at mappedTypes1.js:139, declaration embedded in the .js baseline. JIT `provision_lock_rejects_overlapping_attempts` failed once then passed solo and full rerun: flake, not the tree. Sweep restated: run sweep-89b0455-fresh, binary digest 0fa9235d, +773 with 84 mechanism-indicative (23 class-only + 61 block-only, disjoint), remainder co-occurrence. Follow-ups: ctor and object-literal gates (approved, sequenced next).
  PRESERVATION-CTOR-OBJECT: 2026-09-04 ctor and object gates landed (e6e1f70): injection-free ctor bodies authored on one line keep `{ stmt; }` (class still expands around them); non-empty object literals authored on one line join as `{ a: 1, b: 2 }` with uniform spacing (tight-authored `{a:1}` normalizes). Parameter-property injections always expand. Suites 1819/0 and 598/0, clippy/fmt clean. CLI spots: `const o = { a: 1, b: 2 };` and `constructor() { console.log(1); }` single-line end to end. Next: sweep re-measurement of the 292/579/3 families.
  SWEEP-PRESERVATION: 2026-09-04 full 2048-shard resweep at 2d76b09 (run sweep-2d76b09-preservation, worker binary 9f8c8ce9 asserted pre-flight, clean-tree digest uniform, Node 24.18.0 pinned via PATH; one bash-timeout driver restart, resume skipped footered shards): acceptance 2048/2048 footers, 0 SHARD_FAIL, 0 obligation drift (66786 rows both sides). PASS 13127 -> 13512 (+385), BLOCKING_FAIL 46653 -> 46268 (-385), INAPPLICABLE_LANGUAGE_SERVICE 6693 / INAPPLICABLE_CATALOG_ERROR 312 / TIMEOUT 1 all unchanged (controls). Transitions: 387 BLOCKING_FAIL->PASS, 2 PASS->BLOCKING_FAIL. Churn: compiler/tests/cases/compiler 231, conformance node 16, computedProperties 11, destructuring 10, Accessors 10, templates 8. Range e03e5de..2d76b09 is campaign-level (pin, empty class/block spacing, ctor-close, interface revision, block + ctor/object single-line preservation), not commit-isolated. Regressions, both javascript facet aot mode: override18.ts [noimplicitoverride=true,target=es2015,usedefineforclassfields=true#javascript] and parser768531.ts [allowunusedlabels=true,target=es2015#javascript], each PASS (empty detail) -> BLOCKING_FAIL `javascript structural mismatch after formatting/order normalization`; override18 was the c17ab5e FIELD_INIT conversion (PLUMBING-RESULTS), so the preservation range re-broke it; byte-diff attribution queued (worker request schema: binding, request_id u64, key, observables, argv, working_directory, timeout_ms). Tool local/diff-sweeps.py self-tested (baseline-vs-itself 0 transitions); full list local/sweep-2d76b09-transitions.txt; receipts .outline/evidence-sweep-2d76b09-preservation/.
  SWEEP-PRESERVATION-AUDIT: 2026-09-04 post-push four-digest audit of all 2048 headers under .outline/evidence-sweep-2d76b09-preservation/: exactly one distinct (authority 47d60ce5, candidate_binary 9f8c8ce9, candidate_tree bd95c396, harness 9f323e56) tuple, covering the resume phase too. The entry's uniformity claim is now command-backed, not launch-time inferred.
  REGRESSION-CLOSURE: 2026-09-04 both SWEEP-PRESERVATION regressions root-caused via CLI byte-diff (tsconfig-driven scratch projects mirroring build_tsconfig, strict:true) and fixed. parser768531: the block gate preserved statement-level blocks, but its authority is function-family bodies only — emit_block_with_braces now expands whenever map_open_brace is set (the same context bit that splits brace mapping), commit 2e0e39d. override18: object_literal minted through the untainted node() helper so wholly synthesized defineProperty descriptors collapsed single-line — both mints now record taint, commit 8806887. Post-fix CLI emit matches each baseline's .js section byte-for-byte (wrapper sections aside, which the harness strips). Suites 1821/0 (2 new regression tests) and 598/0, fmt/clippy clean. Next sweep at this HEAD should show 389 net conversions with zero regressions; re-measure before claiming.
  SWEEP-ZERO-REGRESSION: 2026-09-04 confirmation sweep at e8b93bc (run sweep-e8b93bc-preservation, binary e3060e58 pre-flighted, tree 0b2742b6, four-digest audit 1 distinct tuple over 2048 headers, 0 SHARD_FAIL, 0 obligation drift): PASS 13514, BLOCKING_FAIL 46266, controls unchanged. Diff vs 2d76b09: exactly +2 (override18, parser768531 restored), 0 regressions. Diff vs 89b0455-fresh: 387 BLOCKING_FAIL->PASS, 0 PASS->BLOCKING_FAIL — the preservation campaign (e03e5de..e8b93bc) is strictly net-positive. Interim 217b37d sweep caught classThisReference breaking (static init blocks are brace-mapped yet layout-preserving); BlockLayout enum (e8b93bc) names the three contexts, verified byte-for-byte against all three baselines. Receipts .outline/evidence-sweep-e8b93bc-preservation/; transition lists local/sweep-e8b93bc-vs-{2d76b09,89b0455}.txt.
  ID-FLOOR-STRUCTURAL: 2026-09-04 rewriter id seeding rederived from the file's real high-water mark, commit 139656d: one NodeIdSource flows desugar-into-rewriter (alloc_id = ids.fresh), Rewriter::new snapshots the seed as the per-file synthesized floor, PrintSource carries it, and the mint-gate range check reads per-file truth; SYNTHESIZED_ID_FLOOR (the 2d76b09 hazard) is deleted, untransformed prints pass u32::MAX so the range check applies universally. Disjointness of minted vs parser ids is now structural (no constant to cross); the dense-file test pins observables at 1M+ nodes (emit completes, no statement drops, authored single-line preservation holds, descriptor lowering mints). Mutation note: a black-box catch of the old floor is not constructible — taint-window position depends on per-statement id consumption during the walk — so the structural invariant is the guarantee. Suites 1824/0 and 598/0, fmt/clippy/diagnostics clean.
  BUILD-INFO-FACET: 2026-09-04 the 10-cell build-info facet re-derived against authority: zero baselines in the entire 7.0.2 reference tree carry a `//// [name.tsbuildinfo]` section (the harness drops the artifact as environment dependent), so content parity is unprovable and the prior "producer missing" HARNESS_ERROR was a dead end by construction. New contract is emission-shape: the producer emits only when incremental or composite is set (bare tsBuildInfoFile writes nothing, matching tsc's optionsTsBuildInfoFileWithoutIncrementalAndComposite), and the observer's four arms demand artifact-exactly-when-implied (696f9af); the earlier non-empty check was a tautology every cell passed — caught before commit, never landed. Output paths now accept absolute and root-escaping values (resolve_output_from, 413796f): tsc runs incrementalTsBuildInfoFile clean on /a.tsbuildinfo while source paths keep confinement. Verified by direct shard runs at clean HEAD: incrementalOut, incrementalConfig, incrementalTsBuildInfoFile (implied+produced) and optionsTsBuildInfoFileWithoutIncrementalAndComposite (not-implied+none, self-describing artifact) all PASS; 4/10 cells sampled across both arms and both path families, remaining 6 ride the next sweep. Suites 1824/0 and 598/0, fmt/clippy clean.

- [ ] G4: The bamti Node 24 package implements tsc and all thirteen stable native-preview export subpaths, including API-reachable language-service behavior
  CHECK: cargo run -p bamts-verification -- completion verify --root package
  EXPECT: PACKAGE COMPLETE exports=13 cli=PASS api=PASS
  EVIDENCE: 2026-08-28 `completion verify --root package` exit 1: unproven F2.1 through F2.4, cluster:F2, root:package; F2.1 through F2.3 have no checked mutation contract; `typescript-7.0.2:tests/cases/compiler/2dArrays.ts` BLOCKING_FAIL and 19363 more. F2.1 orphan path is gone

- [ ] G5: The ECMAScript runtime passes every applicable test262 obligation in Interpreter, JIT, and AOT modes
  CHECK: cargo run -p bamts-verification -- completion verify --root runtime
  EXPECT: RUNTIME COMPLETE modes=3 blocking=0 external_blocked=0
  EVIDENCE: 2026-08-28 `completion verify --root runtime` exit 1: unproven C0.1, C1.1 through C1.3, C10.1 through C10.6, and 53679 more

- [ ] G6: Interpreter, JIT, AOT, formal correspondence, target cells, and performance evidence satisfy every compiler-product release obligation
  CHECK: cargo run -p bamts-verification -- completion verify --root native
  EXPECT: NATIVE COMPLETE formal=PASS targets=28/28 benchmarks=9/9
  EVIDENCE: 2026-08-28 `completion verify --root native` exit 1: unproven E1.1, E1.2, E2.1 through E2.3, E3.1 through E3.3, E4.1, E4.2, and 389 more

- [ ] G7: The strict TypeScript-product completion gate accepts only receipt-backed PASS states or exact machine-evidenced policy exclusions
  CHECK: cargo run -p bamts-verification -- completion verify --root product
  EXPECT: PRODUCT COMPLETE release=typescript-7.0.2 blocking=0 external_blocked=0 catalog_errors=0
  EVIDENCE: 2026-08-28 `completion verify --root product` exit 1: unproven B0.1, B1.1 through B3.3, and 73626 more. `ledger rebuild --check` reports E_SET_MISMATCH: no canonical receipt set found
  RECEIPTS: `ts_conformance --shards k/N` is deliberately one-based. `bamts-verification suite run --shard i/N` is deliberately zero-based. Workflows convert once before invoking the low-level runner.

- [ ] G8: Repository-required Rust, formal, corpus, npm, workflow, and release checks pass once on the completed tree
  CHECK: just release-gate
  EXPECT: RELEASE GATE PASS
  EVIDENCE: 2026-08-28 not run; the root `Justfile` now defines the ordered fail-closed release gate

- [x] G9: Regenerating every authority-derived artifact is byte-identical to the committed completion evidence
  CHECK: cargo run -p bamts-verification -- completion regenerate --check
  EXPECT: COMPLETION PROGRAM PASS mode=check
  EVIDENCE: 2026-08-28 `cargo run -p bamts-verification --locked -- completion regenerate --check` → `COMPLETION PROGRAM PASS mode=check` (exit 0). A2.7/A3.1/C4.2/F2.1 `owns` match `.outline/type-script-7.0.2-completion.md` Owned paths (`ci.yml`; `verification/evidence/typescript-7.0.2/*.jsonl`; `regexp_v/`; `api_server/`)
