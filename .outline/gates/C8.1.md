# Gates: C8.1 JSON edge (source access? parse reviver done; stringify edge + modules interplay)

Scope: C8.1 owns crates/bamts-runtime/src/builtins/json_edge.rs; it delivers JSON edge (source access? parse reviver done; stringify edge + modules interplay).

- [ ] G1: JSON edge (source access? parse reviver done; stringify edge + modules interplay): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C8.1 --aspect contract
  EXPECT: LEAF C8.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: JSON edge (source access? parse reviver done; stringify edge + modules interplay): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C8.1 --aspect evidence
  EXPECT: LEAF C8.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: JSON edge (source access? parse reviver done; stringify edge + modules interplay): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C8.1 --aspect coverage
  EXPECT: LEAF C8.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: JSON edge (source access? parse reviver done; stringify edge + modules interplay): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C8.1 --aspect regression
  EXPECT: LEAF C8.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: JSON edge (source access? parse reviver done; stringify edge + modules interplay): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C8.1 --aspect mutation
  EXPECT: LEAF C8.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: JSON edge (source access? parse reviver done; stringify edge + modules interplay): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C8.1
  EXPECT: LEAF C8.1 PASS
  EVIDENCE: pending

## Runtime GC/data wave evidence — 2026-08-26

- The dedicated crates/bamts-runtime/src/builtins/json_edge.rs implementation exists, but it is not in the live crate graph after a central-registry overwrite.
- cargo test -p bamts-runtime --lib json_edge: exit 0 but **0 selected, 802 filtered** (artifact://12167). A zero-test filter is not behavioral evidence; C8.1 is not claimed complete here.
- Current central delta: builtins/mod.rs declares mod json, calls json::install, and does not declare json_edge. The clean integration must register json_edge::install as the single JSON owner and remove the legacy json.rs path, then rerun the dedicated filter with a nonzero count.
- The formal G1–G6 checkboxes above remain pending.

## Central integration S1 evidence — 2026-08-26

- `crates/bamts-runtime/src/builtins/mod.rs` now declares and installs
  `json_edge` as the single JSON namespace owner. Its installer applies
  `JSON[Symbol.toStringTag]`; the former registry-side duplicate tag block is
  removed.
- The legacy tracked `crates/bamts-runtime/src/builtins/json.rs` parser and
  stringifier were deleted after a workspace search found no live
  `builtins::json`, `super::json`, `json::install`, or `mod json;` consumer.
  A post-deletion implementation search finds the JSON parser, JSON
  stringifier, and JSON-module parser only in `json_edge.rs`.
- `bigint` is declared as a compile dependency for `json_edge` but is not
  installed in S1. Its stale `EcmaString::from_utf8` calls and the leaf's stale
  string/deletion calls were migrated to `EcmaString::encode` and
  `Machine::delete_property`; the JSON implementation still delegates BigInt
  identification/rejection to the dedicated BigInt owner.
- Focused behavioral coverage includes parse and reviver write-back bypassing
  prototype setters, reviver source/order behavior, replacer-array ordering,
  `toJSON` before replacer/unboxing, cycle rejection, shared-reference
  acceptance, primitive/nested BigInt rejection, wrapper handling, raw JSON,
  gap coercion, and escaping.
- `cargo test -p bamts-runtime --lib json_edge`: exit 0; **18 passed, 0 failed,
  828 filtered** (final post-deletion rerun: `artifact://12913`).
- `cargo check -p bamts-runtime --lib`: exit 0; runtime library checked
  successfully with 85 existing warnings (same final rerun: `artifact://12913`).
- This S1 cutover supplies nonzero behavioral and compile evidence. The formal
  G1–G6 completion-verifier checkboxes remain pending until their declared
  commands run.

## Post-review semantic repair evidence — 2026-08-26

- `JSON.rawJSON` now runs the complete JSON parser over the exact supplied
  text, rejects malformed or trailing input, and rejects parsed object/array
  results. Regression vectors cover `tru`, `0 1`, `null x`, `[1]`, and `{}`;
  only complete primitive JSON texts remain accepted.
- Stringification now unboxes boxed BigInts and rejects them with `TypeError`;
  the previous `{}` wrapper exemption and its expectation were removed.
  `toJSON` still runs first, so an explicit result that is no longer a BigInt
  remains serializable.
- Number and String wrappers in replacer arrays now execute observable
  `ToString` hooks. Number and String wrappers passed as `space` now execute
  observable `ToNumber` and `ToString` respectively. Focused tests assert hook
  side effects, output driven by hook results, and propagation of abrupt
  completions from both space-wrapper coercion paths.
- `cargo test -p bamts-runtime --lib json_edge`: exit 0; **18 passed, 0 failed,
  828 filtered** (`artifact://12963`).
- `cargo check -p bamts-runtime --lib`: exit 0; runtime library checked
  successfully with 85 existing warnings (`artifact://12965`).

## CreateDataProperty hard-stop repair — 2026-08-26

- The leaf-local `create_data_property` now delegates to the runtime's
  canonical `Machine::create_data_property_key` DefineOwnProperty path instead
  of `set_own_data`. JSON parse/reviver/stringify writes therefore create
  writable, enumerable, configurable data properties, can redefine a
  configurable accessor or non-writable own property, and still fail on an
  incompatible non-configurable descriptor.
- The focused reviver regression handles `a` first, replaces later sibling
  `b` with a configurable non-writable/non-enumerable descriptor, then returns
  a replacement while visiting `b`. The parse succeeds and `b` becomes value
  `9` with writable, enumerable, and configurable all restored to `true`.
- `cargo test -p bamts-runtime --lib json_edge`: exit 0; **19 passed, 0 failed,
  828 filtered** (`artifact://12992`).
- `cargo check -p bamts-runtime --lib`: exit 0; runtime library checked
  successfully with 85 existing warnings (`artifact://12994`).

## Verification 2026-08-27 (post-966 restore)

Behavior-complete: `cargo test -p bamts-runtime --lib json_edge` 21/21;
full runtime lib suite 966/966. G1-G6 stay pending (no receipt in
`.outline/evidence/`; II.0.9). Checkboxes unchanged.
