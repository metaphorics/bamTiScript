# Gates: C0.1 $262 host + harness loader + async $DONE + agent/Atomics hooks (blocks every C leaf)

Scope: C0.1 owns crates/bamts-runtime/src/host_objects.rs, crates/bamts-verification/src/oracles/test262_harness.rs; it delivers $262 host + harness loader + async $DONE + agent/Atomics hooks (blocks every C leaf).

- [ ] G1: $262 host + harness loader + async $DONE + agent/Atomics hooks (blocks every C leaf): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C0.1 --aspect contract
  EXPECT: LEAF C0.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: $262 host + harness loader + async $DONE + agent/Atomics hooks (blocks every C leaf): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C0.1 --aspect evidence
  EXPECT: LEAF C0.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: $262 host + harness loader + async $DONE + agent/Atomics hooks (blocks every C leaf): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C0.1 --aspect coverage
  EXPECT: LEAF C0.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: $262 host + harness loader + async $DONE + agent/Atomics hooks (blocks every C leaf): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C0.1 --aspect regression
  EXPECT: LEAF C0.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: $262 host + harness loader + async $DONE + agent/Atomics hooks (blocks every C leaf): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C0.1 --aspect mutation
  EXPECT: LEAF C0.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: $262 host + harness loader + async $DONE + agent/Atomics hooks (blocks every C leaf): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C0.1
  EXPECT: LEAF C0.1 PASS
  EVIDENCE: pending
