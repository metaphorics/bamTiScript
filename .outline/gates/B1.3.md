# Gates: B1.3 parse diagnostic parity (TS1xxx mapping, spans)

Scope: B1.3 owns crates/bamts-compiler/src/diagnostics_parser.rs; it delivers parse diagnostic parity (TS1xxx mapping, spans).

- [ ] G1: parse diagnostic parity (TS1xxx mapping, spans): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.3 --aspect contract
  EXPECT: LEAF B1.3 CONTRACT PASS
  EVIDENCE: blocked: pending — scoped contract proof: cargo test -p bamts-compiler --lib diagnostics_parser::tests (4 passed)

- [ ] G2: parse diagnostic parity (TS1xxx mapping, spans): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.3 --aspect evidence
  EXPECT: LEAF B1.3 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: parse diagnostic parity (TS1xxx mapping, spans): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.3 --aspect coverage
  EXPECT: LEAF B1.3 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: parse diagnostic parity (TS1xxx mapping, spans): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.3 --aspect regression
  EXPECT: LEAF B1.3 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: parse diagnostic parity (TS1xxx mapping, spans): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.3 --aspect mutation
  EXPECT: LEAF B1.3 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: parse diagnostic parity (TS1xxx mapping, spans): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B1.3
  EXPECT: LEAF B1.3 PASS
  EVIDENCE: blocked: scoped tests passed 4 and cargo check -p bamts-compiler passed; leaf-gate rerun pending
