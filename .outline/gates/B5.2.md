# Gates: B5.2 tsconfig full option surface + extends/references parsing parity

Scope: B5.2 owns crates/bamts-compiler/src/project/tsconfig.rs; it delivers tsconfig full option surface + extends/references parsing parity.

- [ ] G1: tsconfig full option surface + extends/references parsing parity: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.2 --aspect contract
  EXPECT: LEAF B5.2 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: tsconfig full option surface + extends/references parsing parity: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.2 --aspect evidence
  EXPECT: LEAF B5.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: tsconfig full option surface + extends/references parsing parity: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.2 --aspect coverage
  EXPECT: LEAF B5.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: tsconfig full option surface + extends/references parsing parity: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.2 --aspect regression
  EXPECT: LEAF B5.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: tsconfig full option surface + extends/references parsing parity: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.2 --aspect mutation
  EXPECT: LEAF B5.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: tsconfig full option surface + extends/references parsing parity: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B5.2
  EXPECT: LEAF B5.2 PASS
  EVIDENCE: blocked: partial 2026-08-26 — cargo check -p bamts-compiler --lib PASS; cargo test -p bamts-compiler --lib project:: PASS (104 passed, 0 failed). Leaf lint/format and receipt aspects remain for the root gate.
