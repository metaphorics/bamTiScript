# Gates: E5.4 stage1.pre-registered-regression guard wired to nightly

Scope: E5.4 owns bench/stage1_regression_guard.rs; it delivers stage1.pre-registered-regression guard wired to nightly.

Artifact lifecycle: `bench/baselines/stage1.json` and
`bench/scorecards/stage1.json` are owned by E5.4 and are intentionally absent.
The runtime does not yet emit `stage1.total_ns`, so no legitimate baseline can
exist; the gate fails closed (`GuardError` → exit 2) until the runtime emits
the counter and an approved capture under `perf/hosts/bh1.toml` conditions
creates the baseline before its measured scorecard. Neither file is ever
hand-fabricated (ADR-005).

- [ ] G1: stage1.pre-registered-regression guard wired to nightly: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.4 --aspect contract
  EXPECT: LEAF E5.4 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: stage1.pre-registered-regression guard wired to nightly: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.4 --aspect evidence
  EXPECT: LEAF E5.4 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: stage1.pre-registered-regression guard wired to nightly: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.4 --aspect coverage
  EXPECT: LEAF E5.4 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: stage1.pre-registered-regression guard wired to nightly: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.4 --aspect regression
  EXPECT: LEAF E5.4 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: stage1.pre-registered-regression guard wired to nightly: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.4 --aspect mutation
  EXPECT: LEAF E5.4 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: stage1.pre-registered-regression guard wired to nightly: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate E5.4
  EXPECT: LEAF E5.4 PASS
  EVIDENCE: pending
