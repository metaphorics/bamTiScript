# Gates: C10.1 locale negotiation + canonicalization core

Scope: C10.1 owns crates/bamts-runtime/src/intl/locale_negotiation.rs; crates/bamts-runtime/src/intl/provider.rs; crates/bamts-runtime/src/intl/locale.rs; it delivers locale negotiation + canonicalization core.

- [ ] G1: locale negotiation + canonicalization core: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.1 --aspect contract
  EXPECT: LEAF C10.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: locale negotiation + canonicalization core: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.1 --aspect evidence
  EXPECT: LEAF C10.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: locale negotiation + canonicalization core: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.1 --aspect coverage
  EXPECT: LEAF C10.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: locale negotiation + canonicalization core: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.1 --aspect regression
  EXPECT: LEAF C10.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: locale negotiation + canonicalization core: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.1 --aspect mutation
  EXPECT: LEAF C10.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: locale negotiation + canonicalization core: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C10.1
  EXPECT: LEAF C10.1 PASS
  EVIDENCE: pending
