# Gates: F2.2 sync, async, filesystem, protocol, project, checker, snapshot, completion, and reference API behavior

Scope: F2.2 owns crates/bamts-compiler/src/service/, npm/bamti/unstable/sync.js, npm/bamti/unstable/async.js, npm/bamti/unstable/fs.js, npm/bamti/unstable/proto.js; it delivers sync, async, filesystem, protocol, project, checker, snapshot, completion, and reference API behavior.

- [ ] G1: sync, async, filesystem, protocol, project, checker, snapshot, completion, and reference API behavior: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F2.2 --aspect contract
  EXPECT: LEAF F2.2 CONTRACT PASS
  EVIDENCE: blocked: scoped contract proof passes: `cargo test -p bamts-compiler --lib service` -> 19 passed, 0 failed; every persistent document mutation now crosses a final cancellation commit boundary: new documents check immediately before source-ID allocation and insertion, while replacements check immediately before insertion. The focused `cancelled_commit_new_document_leaves_state_untouched` and `cancelled_commit_replacement_document_leaves_state_untouched` regressions prove pre-cancelled direct commits leave the full document snapshot and `next_source_id` unchanged. The shared `bamts_cancel::CancellationToken` reaches scan, parse, and checker work through sync and async service calls; existing pre-cancelled open, snapshot-pipeline cancellation, and sync/async query parity remain green. All four owned JavaScript entry points previously passed `node --check`; loader-isolated imports of sync, async, fs, and proto passed; a fake-transport smoke proved async methods set `async: true` without mutating caller params and sync disposal remains ordered after queued requests. The prescribed completion verifier was not run by this scoped package/API slice, so G1 remains open.

- [ ] G2: sync, async, filesystem, protocol, project, checker, snapshot, completion, and reference API behavior: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F2.2 --aspect evidence
  EXPECT: LEAF F2.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: sync, async, filesystem, protocol, project, checker, snapshot, completion, and reference API behavior: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F2.2 --aspect coverage
  EXPECT: LEAF F2.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: sync, async, filesystem, protocol, project, checker, snapshot, completion, and reference API behavior: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F2.2 --aspect regression
  EXPECT: LEAF F2.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: sync, async, filesystem, protocol, project, checker, snapshot, completion, and reference API behavior: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F2.2 --aspect mutation
  EXPECT: LEAF F2.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: sync, async, filesystem, protocol, project, checker, snapshot, completion, and reference API behavior: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate F2.2
  EXPECT: LEAF F2.2 PASS
  EVIDENCE: blocked: owned scoped behavioral check passes: `cargo test -p bamts-compiler --lib service` -> 19 passed, 0 failed, including deterministic final-commit cancellation tests for both new and replacement documents, pre-cancelled open atomicity, snapshot-pipeline cancellation, and sync/async parity. Both direct commit regressions prove the document snapshot and `next_source_id` remain unchanged on cancellation. Previously recorded JavaScript evidence remains: `node --check npm/bamti/unstable/{sync,async,fs,proto}.js` passes; loader-isolated module imports and sync/async fake-transport behavior smoke pass. The prescribed `just leaf-gate F2.2` was not run under the assignment's no-project-wide-validator constraint, so G6 remains open.

## Transport rebuild evidence

- `cargo test -p bamts-compiler --lib service` — 19 passed, 0 failed, 1527 filtered after the transport cutover.
- The new `Session::plan` checks cancellation before `Session::apply`; admitted request tickets carry the same `CancellationToken` into scanner, parser, checker, service, and public-AST paths. The API-server module suite includes pre-dispatch cancellation, executing cancellation, AST scanner/visitor cancellation, late cancellation, ID reuse, and session-state preservation coverage.
- The prescribed completion verifier and `just leaf-gate F2.2` were not run, so the gate checkboxes remain open.
