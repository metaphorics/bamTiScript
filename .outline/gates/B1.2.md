# Gates: B1.2 grammar residual closure (lane-driven: import attributes, decorator forms, regexp-v syntax)

Scope: B1.2 owns crates/bamts-compiler/src/parser.rs; it delivers grammar residual closure (lane-driven: import attributes, decorator forms, regexp-v syntax).

- [ ] G1: grammar residual closure (lane-driven: import attributes, decorator forms, regexp-v syntax): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.2 --aspect contract
  EXPECT: LEAF B1.2 CONTRACT PASS
  EVIDENCE: blocked: pending — scoped contract proof: cargo test -p bamts-compiler --lib parser::tests (86 passed)

- [ ] G2: grammar residual closure (lane-driven: import attributes, decorator forms, regexp-v syntax): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.2 --aspect evidence
  EXPECT: LEAF B1.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: grammar residual closure (lane-driven: import attributes, decorator forms, regexp-v syntax): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.2 --aspect coverage
  EXPECT: LEAF B1.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: grammar residual closure (lane-driven: import attributes, decorator forms, regexp-v syntax): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.2 --aspect regression
  EXPECT: LEAF B1.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: grammar residual closure (lane-driven: import attributes, decorator forms, regexp-v syntax): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.2 --aspect mutation
  EXPECT: LEAF B1.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: grammar residual closure (lane-driven: import attributes, decorator forms, regexp-v syntax): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B1.2
  EXPECT: LEAF B1.2 PASS
  EVIDENCE: blocked: scoped tests passed 86 and cargo check -p bamts-compiler passed; leaf-gate rerun pending
