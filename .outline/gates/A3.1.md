# Gates: A3.1 scoped baseline measurement and honest ledger rebuild

Scope: A3.1 owns verification/evidence/typescript-7.0.2/*.jsonl and proof/completeness-ledger.json; it measures every catalog in the TypeScript-product root without pulling unrelated full-Node catalogs into completion.

- [ ] G1: The declared TypeScript-product catalog set matches the frozen root contract
  CHECK: cargo run -p bamts-verification -- completion verify --leaf A3.1 --aspect contract
  EXPECT: LEAF A3.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: Every selected logical obligation has a current content-addressed baseline receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf A3.1 --aspect evidence
  EXPECT: LEAF A3.1 EVIDENCE PASS
EVIDENCE: 2026-09-03 sweep complete: 2048/2048 shards merged to verification/evidence/typescript-7.0.2/typescript-7.0.2-compiler-x86_64-unknown-linux-gnu.jsonl (0b5bdde, 66786 rows, v2); v1 shards deleted, both named obligations proven re-measured in the merged set. Aspect still exit 1: G2 lacks current content-addressed evidence, receipt binds pre-commit tree f8c28dc and is stale under 0b5bdde. Rebuild admits zero rows; see G6.

- [ ] G3: The baseline records every non-PASS state honestly and has no lane-infrastructure error
  CHECK: cargo run -p bamts-verification -- completion verify --leaf A3.1 --aspect coverage
  EXPECT: LEAF A3.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: No previously passing obligation regressed during baseline regeneration
  CHECK: cargo run -p bamts-verification -- completion verify --leaf A3.1 --aspect regression
  EXPECT: LEAF A3.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: Deleting a receipt or adding an undeclared catalog row makes baseline verification fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf A3.1 --aspect mutation
  EXPECT: LEAF A3.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: Baseline generation and ledger rebuild pass their narrow repository checks
  CHECK: just leaf-gate A3.1
  EXPECT: LEAF A3.1 PASS
EVIDENCE: 2026-09-03 structurally blocked, measured two ways: ledger rebuild --write exit 1 E_SET_MISMATCH with 248828 rejections (merged receipt StaleBinding on candidate_tree_digest plus unreceipted benchmark/formal/test262 obligations); re-running suite merge on 0b5bdde exit 1 E_DIGEST, shards stale for the current binding. Landing a receipt changes HEAD^{tree} which the receipt binds, so no committed receipt can be admitted; guards at crates/bamts-verification/src/rebuild.rs:613-624 plus the dirty-tree gate (28dc143, postdates ledger 5e83845). Needs an A2.6-scope landing-sequence design decision.
