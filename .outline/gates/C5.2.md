# Gates: C5.2 async iterators/generators + for-await-of + TLA

Scope: C5.2 owns crates/bamts-runtime/src/vm/async_iterators.rs; it delivers async iterators/generators + for-await-of + TLA.

- [ ] G1: async iterators/generators + for-await-of + TLA: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.2 --aspect contract
  EXPECT: LEAF C5.2 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: async iterators/generators + for-await-of + TLA: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.2 --aspect evidence
  EXPECT: LEAF C5.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: async iterators/generators + for-await-of + TLA: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.2 --aspect coverage
  EXPECT: LEAF C5.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: async iterators/generators + for-await-of + TLA: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.2 --aspect regression
  EXPECT: LEAF C5.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: async iterators/generators + for-await-of + TLA: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.2 --aspect mutation
  EXPECT: LEAF C5.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: async iterators/generators + for-await-of + TLA: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.2 --aspect regression
  EXPECT: LEAF C5.2 REGRESSION PASS
  EVIDENCE: pending

## Verification 2026-08-27 (post-966 restore)

Behavior-complete: `cargo test -p bamts-runtime --lib async_iterators` 6/6;
full runtime lib suite 966/966. G1-G6 stay pending (no receipt in
`.outline/evidence/`; II.0.9). Checkboxes unchanged.
