# Gates: C3.1 Array ES2023+ (with/toSorted/toSpliced/toReversed, fromAsync) + iterator helpers

Scope: C3.1 owns crates/bamts-runtime/src/builtins/array_es2023.rs; it delivers Array ES2023+ (with/toSorted/toSpliced/toReversed, fromAsync) + iterator helpers.

- [ ] G1: Array ES2023+ (with/toSorted/toSpliced/toReversed, fromAsync) + iterator helpers: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.1 --aspect contract
  EXPECT: LEAF C3.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: Array ES2023+ (with/toSorted/toSpliced/toReversed, fromAsync) + iterator helpers: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.1 --aspect evidence
  EXPECT: LEAF C3.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: Array ES2023+ (with/toSorted/toSpliced/toReversed, fromAsync) + iterator helpers: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.1 --aspect coverage
  EXPECT: LEAF C3.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: Array ES2023+ (with/toSorted/toSpliced/toReversed, fromAsync) + iterator helpers: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.1 --aspect regression
  EXPECT: LEAF C3.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: Array ES2023+ (with/toSorted/toSpliced/toReversed, fromAsync) + iterator helpers: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.1 --aspect mutation
  EXPECT: LEAF C3.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: Array ES2023+ (with/toSorted/toSpliced/toReversed, fromAsync) + iterator helpers: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C3.1
  EXPECT: LEAF C3.1 PASS
  EVIDENCE: pending

## 2026-08-26 runtime-collections leaf wave

- SOURCE: `array_es2023.rs` is registered and now uses own data-property creation for copying results while retaining intrinsic `ArrayCreate`, read-through-holes, stable sorting, omitted-delete-count, negative-index, abrupt-completion, and source-immutability contracts.
- FOCUSED CHECK: `cargo test -p bamts-runtime builtins::array_es2023::tests -- --nocapture`.
- OBSERVED: **not verified**. The latest run exited 101 before the filter because concurrent `builtins/annex_b.rs:49` references missing `install_regexp_legacy_statics` (`artifact://12069`). No gate above is checked from this run.
- WIRING: already active through `builtins/mod.rs` and `array.rs`; no C3.1 central delta remains.
