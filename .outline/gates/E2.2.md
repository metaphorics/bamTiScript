# Gates: E2.2 JIT tiering: OSR + deopt + warmup policy

Scope: E2.2 owns crates/bamts-codegen/src/jit/tiering.rs; it delivers JIT tiering: OSR + deopt + warmup policy.

- [ ] G1: JIT tiering: OSR + deopt + warmup policy: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E2.2 --aspect contract
  EXPECT: LEAF E2.2 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: JIT tiering: OSR + deopt + warmup policy: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E2.2 --aspect evidence
  EXPECT: LEAF E2.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: JIT tiering: OSR + deopt + warmup policy: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E2.2 --aspect coverage
  EXPECT: LEAF E2.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: JIT tiering: OSR + deopt + warmup policy: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E2.2 --aspect regression
  EXPECT: LEAF E2.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: JIT tiering: OSR + deopt + warmup policy: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E2.2 --aspect mutation
  EXPECT: LEAF E2.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: JIT tiering: OSR + deopt + warmup policy: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate E2.2
  EXPECT: LEAF E2.2 PASS
  EVIDENCE: pending
