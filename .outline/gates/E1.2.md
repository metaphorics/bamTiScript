# Gates: E1.2 bytecode verifier co-extension (definite-init/bounds per ISA change)

Scope: E1.2 owns crates/bamts-bytecode/src/verifier.rs and the root verifier/dataflow integration in crates/bamts-bytecode/src/lib.rs; it delivers verifier co-extension for each E1.1 opcode.

- [ ] G1: bytecode verifier co-extension (definite-init/bounds per ISA change): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E1.2 --aspect contract
  EXPECT: LEAF E1.2 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: bytecode verifier co-extension (definite-init/bounds per ISA change): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E1.2 --aspect evidence
  EXPECT: LEAF E1.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: bytecode verifier co-extension (definite-init/bounds per ISA change): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E1.2 --aspect coverage
  EXPECT: LEAF E1.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: bytecode verifier co-extension (definite-init/bounds per ISA change): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E1.2 --aspect regression
  EXPECT: LEAF E1.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: bytecode verifier co-extension (definite-init/bounds per ISA change): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E1.2 --aspect mutation
  EXPECT: LEAF E1.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: bytecode verifier co-extension (definite-init/bounds per ISA change): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate E1.2
  EXPECT: LEAF E1.2 PASS
  EVIDENCE: pending

Focused implementation evidence (2026-08-26):
- Every tag 52..=57 participates in register-read/write facts, fall-through CFG validation, register and constant bounds, string-constant typing where applicable, definite initialization, and CopyDataProperties alias rejection.
- Root malformed-bytecode regressions cover truncated extension operands and unknown tag 58; extension regressions also cover non-canonical/overflowing LEB128, out-of-bounds operands, wrong constant type, aliased copy operands, and read-before-write.
- The ISA is register-based and stackless; no operand-stack invariant exists to extend. PC values index decoded instructions, so a jump into an encoded operand byte is unrepresentable after decoding; jump bounds remain checked against decoded instruction count.
- cargo test -p bamts-bytecode --lib: PASS, 132 passed, 0 failed.
- cargo check -p bamts-bytecode --lib: PASS.
- cargo clippy -p bamts-bytecode --all-targets -- -D warnings: PASS.
- cargo fmt -p bamts-bytecode -- --check: PASS.
- G1-G6 remain unchecked because their exact completion/receipt/mutation/leaf-gate commands were not run in this shared-tree wave; focused crate evidence does not substitute for those receipts.
