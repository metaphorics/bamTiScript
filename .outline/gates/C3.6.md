# Gates: C3.6 Set methods (union/intersection/difference/...)

Scope: C3.6 owns crates/bamts-runtime/src/builtins/set_methods.rs; it delivers Set methods (union/intersection/difference/...).

- [ ] G1: Set methods (union/intersection/difference/...): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.6 --aspect contract
  EXPECT: LEAF C3.6 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: Set methods (union/intersection/difference/...): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.6 --aspect evidence
  EXPECT: LEAF C3.6 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: Set methods (union/intersection/difference/...): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.6 --aspect coverage
  EXPECT: LEAF C3.6 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: Set methods (union/intersection/difference/...): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.6 --aspect regression
  EXPECT: LEAF C3.6 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: Set methods (union/intersection/difference/...): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.6 --aspect mutation
  EXPECT: LEAF C3.6 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: Set methods (union/intersection/difference/...): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C3.6
  EXPECT: LEAF C3.6 PASS
  EVIDENCE: pending

## 2026-08-26 runtime-collections leaf wave

- SOURCE: `set_methods.rs` preserves the current ECMA-262 set-like protocol for all seven methods, including receiver-first validation, size/has/keys observation, size-driven branches, SameValueZero, iterator closing, mutation visibility, duplicate suppression, and result ordering; adversarial tests cover these boundaries.
- FOCUSED CHECK: `cargo test -p bamts-runtime builtins::set_methods::tests -- --nocapture`.
- OBSERVED: **not verified**. A clean central interval selected zero tests because the leaf is orphaned; the latest run exited 101 in unrelated `builtins/annex_b.rs:49` (`artifact://12088`). Zero selected tests are not evidence.
- WIRING: add `mod set_methods;` and call `set_methods::install` from `collections::install_set`; existing Set storage, BuiltinTable cache, GC, native, and property carriers are sufficient.
