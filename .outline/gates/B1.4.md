# Gates: B1.4 scanner edge parity (unicode/separators/bigint/rescans)

Scope: B1.4 owns crates/bamts-compiler/src/scanner.rs; it delivers scanner edge parity (unicode/separators/bigint/rescans).

- [ ] G1: scanner edge parity (unicode/separators/bigint/rescans): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.4 --aspect contract
  EXPECT: LEAF B1.4 CONTRACT PASS
  EVIDENCE: blocked: pending — scoped contract proof: cargo test -p bamts-compiler --lib scanner::tests (38 passed)

- [ ] G2: scanner edge parity (unicode/separators/bigint/rescans): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.4 --aspect evidence
  EXPECT: LEAF B1.4 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: scanner edge parity (unicode/separators/bigint/rescans): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.4 --aspect coverage
  EXPECT: LEAF B1.4 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: scanner edge parity (unicode/separators/bigint/rescans): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.4 --aspect regression
  EXPECT: LEAF B1.4 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: scanner edge parity (unicode/separators/bigint/rescans): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.4 --aspect mutation
  EXPECT: LEAF B1.4 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: scanner edge parity (unicode/separators/bigint/rescans): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B1.4
  EXPECT: LEAF B1.4 PASS
  EVIDENCE: blocked: scoped tests passed 38 and cargo check -p bamts-compiler passed; leaf-gate rerun pending
