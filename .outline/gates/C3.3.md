# Gates: C3.3 DataView full

Scope: C3.3 owns crates/bamts-runtime/src/builtins/dataview.rs; it delivers DataView full.

- [ ] G1: DataView full: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.3 --aspect contract
  EXPECT: LEAF C3.3 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: DataView full: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.3 --aspect evidence
  EXPECT: LEAF C3.3 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: DataView full: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.3 --aspect coverage
  EXPECT: LEAF C3.3 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: DataView full: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.3 --aspect regression
  EXPECT: LEAF C3.3 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: DataView full: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.3 --aspect mutation
  EXPECT: LEAF C3.3 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: DataView full: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C3.3
  EXPECT: LEAF C3.3 PASS
  EVIDENCE: pending

## 2026-08-26 runtime-collections leaf wave

- SOURCE: `dataview.rs` now contains the full Number/BigInt getter-setter matrix, default big-endian and explicit little-endian behavior, constructor revalidation, detached checks, and distinct resized-out-of-bounds versus relative-offset errors.
- FOCUSED CHECK: `cargo test -p bamts-runtime builtins::dataview::tests -- --nocapture`.
- OBSERVED: **not verified**. A clean central interval selected zero tests because the leaf is orphaned; the latest run then exited 101 in unrelated `builtins/annex_b.rs:49` (`artifact://12078`). Zero selected tests are not evidence.
- WIRING: requires module/installer registration, `BuiltinTable.dataview_prototype`, `HeapEntry::DataView`, ArrayBuffer.isView recognition, and exhaustive GC/native/property/accounting arms.

## Verification 2026-08-27 (post-966 restore)

Behavior-complete. `cargo test -p bamts-runtime --lib dataview` is 16/16 (15
`builtins::dataview::tests` plus `structured_clone::tests::dataview_is_classified_as_an_unsupported_binary_view`).
Dedicated module `builtins::dataview::tests` is 15/15. Full runtime lib suite
966/966. `HeapEntry::DataView` is present and `mod dataview` is declared.
G1-G6 stay pending: no content-addressed receipt in `.outline/evidence/`;
closure deferred to II.0.9. No receipt fabricated; checkboxes unchanged.
