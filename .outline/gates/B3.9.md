# Gates: B3.9 decorators semantics (stage-3 + legacy experimental)

Scope: B3.9 owns crates/bamts-compiler/src/checker/decorators.rs; it delivers decorators semantics (stage-3 + legacy experimental).

- [ ] G1: decorators semantics (stage-3 + legacy experimental): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.9 --aspect contract
  EXPECT: LEAF B3.9 CONTRACT PASS
  EVIDENCE: blocked: `checker/decorators.rs` implements legacy/stage-3 target, option, export-placement, call-argument, and return-contract checks through canonical `TypeTable::assignable`; `cargo test -p bamts-compiler --lib checker::decorators` passed all 15 focused contract tests on 2026-08-25.

- [ ] G2: decorators semantics (stage-3 + legacy experimental): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.9 --aspect evidence
  EXPECT: LEAF B3.9 EVIDENCE PASS
  EVIDENCE: blocked: pending; content-addressed receipts are owned by the integrated completion gate and were not generated in this leaf recovery.

- [ ] G3: decorators semantics (stage-3 + legacy experimental): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.9 --aspect coverage
  EXPECT: LEAF B3.9 COVERAGE PASS
  EVIDENCE: blocked: 15 passing focused tests cover legacy class/member/property/parameter signatures, stage-3 value/context and auto-accessor behavior, non-callable/argument/return failures, ambient/static/constructor/accessor/parameter restrictions, both export positions, and option conflicts with paired accepted cases.

- [ ] G4: decorators semantics (stage-3 + legacy experimental): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.9 --aspect regression
  EXPECT: LEAF B3.9 REGRESSION PASS
  EVIDENCE: blocked: Every rejection rule in `checker::decorators::tests` has a passing clean accepted counterpart; the repository completion regression aspect remains for the integrated gate.

- [ ] G5: decorators semantics (stage-3 + legacy experimental): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.9 --aspect mutation
  EXPECT: LEAF B3.9 MUTATION PASS
  EVIDENCE: blocked: pending; no mutation receipt was fabricated or claimed.

- [ ] G6: decorators semantics (stage-3 + legacy experimental): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B3.9
  EXPECT: LEAF B3.9 PASS
  EVIDENCE: blocked: UNMET: canonical `just leaf-gate B3.9` runner is absent. Scoped proof on 2026-08-26: decorator tests passed 15/15, `cargo check -p bamts-compiler --lib` passed, and integrated checker tests passed 739/739. G1-G5 remain unchecked; no completion or mutation receipt was fabricated.
