# Gates: B6.1 tsc argv surface + exit-code parity

Scope: B6.1 owns crates/bamts-cli/src/cli/tsc_args.rs; it delivers tsc argv surface + exit-code parity.

- [x] G1: tsc argv surface + exit-code parity: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B6.1 --aspect contract
  EXPECT: LEAF B6.1 CONTRACT PASS
  EVIDENCE: receipt=.outline/evidence/B6.1-contract.json sha256=a815753718155ae6fe2417c220fbdd99d4c7be0ee491e3a24aafb334173ac49b
  Contract detail: TARGETS starts at es6; invalid_enum_lists_allowed_values and removed_es5_target_is_rejected reject 'es5' and list 'es6'..'esnext'. Live /tmp/bamts-tsc-7.0.2: --target es3/esBOGUS TS6046 starting es6; --target es5 TS5108; --target es6 status 0. cargo test -p bamts-cli --lib tsc_args did not compile (bamts-codegen E0080/E0004 outside owns).

- [ ] G2: tsc argv surface + exit-code parity: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B6.1 --aspect evidence
  EXPECT: LEAF B6.1 EVIDENCE PASS
  EVIDENCE: blocked: blocked on program-wide completeness ledger: proof/completeness-ledger.json manifest digest is stale against verification/manifest.lock.json (StaleEvidence); typescript-7.0.2 has 12782 BLOCKING_FAIL rows owned by P1 and zero PASS rows in the ledger. Not B6-scope.

- [ ] G3: tsc argv surface + exit-code parity: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B6.1 --aspect coverage
  EXPECT: LEAF B6.1 COVERAGE PASS
  EVIDENCE: blocked: blocked on program-wide completeness ledger: proof/completeness-ledger.json manifest digest is stale against verification/manifest.lock.json (StaleEvidence); typescript-7.0.2 has 12782 BLOCKING_FAIL rows owned by P1 and zero PASS rows in the ledger. Not B6-scope.

- [ ] G4: tsc argv surface + exit-code parity: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B6.1 --aspect regression
  EXPECT: LEAF B6.1 REGRESSION PASS
  EVIDENCE: blocked: blocked on program-wide completeness ledger: proof/completeness-ledger.json manifest digest is stale against verification/manifest.lock.json (StaleEvidence); typescript-7.0.2 has 12782 BLOCKING_FAIL rows owned by P1 and zero PASS rows in the ledger. Not B6-scope.

- [ ] G5: tsc argv surface + exit-code parity: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B6.1 --aspect mutation
  EXPECT: LEAF B6.1 MUTATION PASS
  EVIDENCE: blocked: mutation receipt recorded at .outline/evidence/B6.1-mutation.json; inserted es5 into TARGETS; guard tests were not reached: cargo test failed compiling bamts-codegen (E0080/E0004). Box left open until those guards fail under the mutation.

- [ ] G6: tsc argv surface + exit-code parity: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: cargo fmt -p bamts-cli -- --check ; cargo check --offline -p bamts-cli ; cargo check --offline -p bamts-compiler ; cargo clippy --offline -p bamts-cli --all-targets -- -D warnings ; cargo test --offline -p bamts-cli --lib -- tsc_args
  EXPECT: LEAF B6.1 PASS
  EVIDENCE: blocked: cargo check --offline -p bamts-compiler finished. cargo check --offline -p bamts-cli and cargo test --offline -p bamts-cli --lib -- tsc_args fail compiling bamts-codegen (E0080 helper table is dense; E0004 GetSuper/SetSuper/ImportAttributes); leaf-gate rerun pending.

## Workstream evidence (not gate closure)

- Authority: `/tmp/bamts-tsc-7.0.2`, built from pinned checkout commit `1e4744d68260a7cb91b62b12edc3f6a2187faaf1`, reported `Version 7.0.2`.
- `--target es5 --ignoreConfig` is rejected (TS5108). `--target es3` and unknown values emit TS6046 with allowed values starting at `'es6'`. `--target es6` is accepted (status 0).
- `TARGETS` in `tsc_args.rs` starts at `es6` (es5 remains a `--lib` value). Tests assert the TS6046 list contains `'es6'`/`'esnext'` and does not contain `'es5'`.
- `cargo test --offline -p bamts-cli --lib -- tsc_args` cannot compile until bamts-codegen opcode/helper integration lands.
