# Gates: C11.1 Instant + PlainTime + Duration core arithmetic

Scope: C11.1 owns crates/bamts-runtime/src/temporal/instant_duration.rs; it delivers Instant + PlainTime + Duration core arithmetic.

- [ ] G1: Instant + PlainTime + Duration core arithmetic: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.1 --aspect contract
  EXPECT: LEAF C11.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: Instant + PlainTime + Duration core arithmetic: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.1 --aspect evidence
  EXPECT: LEAF C11.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: Instant + PlainTime + Duration core arithmetic: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.1 --aspect coverage
  EXPECT: LEAF C11.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: Instant + PlainTime + Duration core arithmetic: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.1 --aspect regression
  EXPECT: LEAF C11.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: Instant + PlainTime + Duration core arithmetic: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.1 --aspect mutation
  EXPECT: LEAF C11.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: Instant + PlainTime + Duration core arithmetic: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C11.1
  EXPECT: LEAF C11.1 PASS
  EVIDENCE: pending
