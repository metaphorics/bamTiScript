# Gates: C1.2 Number formatting/parsing exactness (toFixed/toPrecision/toExponential/radix)

Scope: C1.2 owns crates/bamts-runtime/src/builtins/number_format.rs; it delivers Number formatting/parsing exactness (toFixed/toPrecision/toExponential/radix).

- [ ] G1: Number formatting/parsing exactness (toFixed/toPrecision/toExponential/radix): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C1.2 --aspect contract
  EXPECT: LEAF C1.2 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: Number formatting/parsing exactness (toFixed/toPrecision/toExponential/radix): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C1.2 --aspect evidence
  EXPECT: LEAF C1.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: Number formatting/parsing exactness (toFixed/toPrecision/toExponential/radix): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C1.2 --aspect coverage
  EXPECT: LEAF C1.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: Number formatting/parsing exactness (toFixed/toPrecision/toExponential/radix): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C1.2 --aspect regression
  EXPECT: LEAF C1.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: Number formatting/parsing exactness (toFixed/toPrecision/toExponential/radix): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C1.2 --aspect mutation
  EXPECT: LEAF C1.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: Number formatting/parsing exactness (toFixed/toPrecision/toExponential/radix): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C1.2
  EXPECT: LEAF C1.2 PASS
  EVIDENCE: pending
