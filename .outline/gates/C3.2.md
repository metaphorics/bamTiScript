# Gates: C3.2 TypedArray full family (Int8/Uint8Clamped/Int16/Uint16/Int32/Uint32/Float32/Float64/BigInt64/BigUint64 + %TypedArray% shared methods)

Scope: C3.2 owns crates/bamts-runtime/src/builtins/typedarray_all.rs; it delivers TypedArray full family (Int8/Uint8Clamped/Int16/Uint16/Int32/Uint32/Float32/Float64/BigInt64/BigUint64 + %TypedArray% shared methods).

- [ ] G1: TypedArray full family (Int8/Uint8Clamped/Int16/Uint16/Int32/Uint32/Float32/Float64/BigInt64/BigUint64 + %TypedArray% shared methods): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.2 --aspect contract
  EXPECT: LEAF C3.2 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: TypedArray full family (Int8/Uint8Clamped/Int16/Uint16/Int32/Uint32/Float32/Float64/BigInt64/BigUint64 + %TypedArray% shared methods): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.2 --aspect evidence
  EXPECT: LEAF C3.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: TypedArray full family (Int8/Uint8Clamped/Int16/Uint16/Int32/Uint32/Float32/Float64/BigInt64/BigUint64 + %TypedArray% shared methods): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.2 --aspect coverage
  EXPECT: LEAF C3.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: TypedArray full family (Int8/Uint8Clamped/Int16/Uint16/Int32/Uint32/Float32/Float64/BigInt64/BigUint64 + %TypedArray% shared methods): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.2 --aspect regression
  EXPECT: LEAF C3.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: TypedArray full family (Int8/Uint8Clamped/Int16/Uint16/Int32/Uint32/Float32/Float64/BigInt64/BigUint64 + %TypedArray% shared methods): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.2 --aspect mutation
  EXPECT: LEAF C3.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: TypedArray full family (Int8/Uint8Clamped/Int16/Uint16/Int32/Uint32/Float32/Float64/BigInt64/BigUint64 + %TypedArray% shared methods): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C3.2
  EXPECT: LEAF C3.2 PASS
  EVIDENCE: pending

## 2026-08-26 runtime-collections leaf wave

- SOURCE: `typedarray_all.rs` now carries all 12 element families, live detached/resizable view validation, Number/BigInt content separation, exact conversions, shared methods, species/intrinsic-copy distinctions, overlap handling, and base64/hex boundaries.
- FOCUSED CHECK: `cargo test -p bamts-runtime builtins::typedarray_all::tests -- --nocapture`.
- OBSERVED: **not verified**. The latest run exited 101 in unrelated `builtins/annex_b.rs:49` before leaf compilation (`artifact://12076`); the module is also still orphaned, so a later nonzero run is mandatory.
- WIRING: requires module/installer registration, replacing the legacy `uint8array` installer, TypedArray `BuiltinTable` caches, `HeapEntry::TypedArray`, and exhaustive GC/native/property/accounting arms.
