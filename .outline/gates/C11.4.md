# Gates: C11.4 Now + rounding/balancing + Intl interplay + toString/serialization

Scope: C11.4 owns crates/bamts-runtime/src/temporal/now_rounding.rs; it delivers Now + rounding/balancing + Intl interplay + toString/serialization.

- [ ] G1: Now + rounding/balancing + Intl interplay + toString/serialization: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.4 --aspect contract
  EXPECT: LEAF C11.4 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: Now + rounding/balancing + Intl interplay + toString/serialization: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.4 --aspect evidence
  EXPECT: LEAF C11.4 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: Now + rounding/balancing + Intl interplay + toString/serialization: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.4 --aspect coverage
  EXPECT: LEAF C11.4 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: Now + rounding/balancing + Intl interplay + toString/serialization: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.4 --aspect regression
  EXPECT: LEAF C11.4 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: Now + rounding/balancing + Intl interplay + toString/serialization: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.4 --aspect mutation
  EXPECT: LEAF C11.4 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: Now + rounding/balancing + Intl interplay + toString/serialization: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C11.4
  EXPECT: LEAF C11.4 PASS
  EVIDENCE: pending
