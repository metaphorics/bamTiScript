# Gates: E3.2 AOT linking + cache correctness (existing fallback-cache lessons)

Scope: E3.2 owns crates/bamts-codegen/src/aot/linking.rs; it delivers AOT linking + cache correctness (existing fallback-cache lessons).

- [ ] G1: AOT linking + cache correctness (existing fallback-cache lessons): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E3.2 --aspect contract
  EXPECT: LEAF E3.2 CONTRACT PASS
  EVIDENCE: blocked: 2026-08-26 ultimate production boundary implemented and verified where the shared tree permits: `cargo test -p bamts-codegen --features aot --lib aot` passed 29 tests and `cargo test -p bamts-verification --lib toolchain_schema` passed 11 tests. LinkPlan binds deterministic argv/environment ordering, output path, exact content digests for every object/archive input, canonical linker resolution, attested executable bytes, and successful `--version` output. The CLI probes and links through one retained private copy of the attested bytes with the original argv[0], preserving GCC driver behavior while eliminating configured-path replacement between probe and spawn. Linked images are parsed before publication and any writable+executable mapped segment is rejected. Regressions cover same-banner replacement, same-path archive-content changes, output-path changes, and synthetic W+X images. The fresh CLI link rerun was attempted but its test crate is currently blocked before driver tests by unrelated `api_server.rs` `PipeReader: BufRead` errors; no receipt was fabricated.

- [ ] G2: AOT linking + cache correctness (existing fallback-cache lessons): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E3.2 --aspect evidence
  EXPECT: LEAF E3.2 EVIDENCE PASS
  EVIDENCE: blocked: pending Main's content-addressed receipt registration and integrated evidence verifier; no receipt was fabricated by this implementation worker.

- [ ] G3: AOT linking + cache correctness (existing fallback-cache lessons): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E3.2 --aspect coverage
  EXPECT: LEAF E3.2 COVERAGE PASS
  EVIDENCE: blocked: pending integrated coverage verifier; current ultimate-boundary codegen AOT result is 29 passed and verification toolchain-schema result is 11 passed, both with 0 ignored. CLI link rerun was attempted but externally blocked during unrelated API-server test compilation.

- [ ] G4: AOT linking + cache correctness (existing fallback-cache lessons): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E3.2 --aspect regression
  EXPECT: LEAF E3.2 REGRESSION PASS
  EVIDENCE: blocked: pending integrated regression verifier; codegen AOT passed 29 tests and verification toolchain-schema passed 11 after the ultimate boundary changes. The prior CLI link result was 8 passed; the required fresh rerun was attempted but did not reach driver tests because of the unrelated API-server test errors recorded above.

- [ ] G5: AOT linking + cache correctness (existing fallback-cache lessons): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E3.2 --aspect mutation
  EXPECT: LEAF E3.2 MUTATION PASS
  EVIDENCE: blocked: pending Main's registered mutation and mutation verifier; no fake mutation PASS evidence recorded.

- [ ] G6: AOT linking + cache correctness (existing fallback-cache lessons): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate E3.2
  EXPECT: LEAF E3.2 PASS
  EVIDENCE: blocked: 2026-08-26 codegen AOT passed 29 focused tests and verification toolchain-schema passed 11. Fresh CLI link was attempted but blocked before driver tests by concurrent API-server test compilation; a later production check was independently blocked by concurrent runtime JSON/BigInt integration errors. `just leaf-gate E3.2` remains unrun; this row stays unchecked.
