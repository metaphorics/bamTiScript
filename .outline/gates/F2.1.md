# Gates: F2.1 bamti package manifest, tsc executable, persistent API transport, and thirteen-subpath export map

Scope: F2.1 owns npm/bamti/package.json, npm/bamti/index.js, npm/bamti/index.d.ts, npm/bamti/internal transport files, and crates/bamts-cli/src/api_server/; it delivers the bamti package manifest, tsc executable, bounded persistent API transport, Node error mapping, and thirteen-subpath export map.

- [x] G1: bamti package manifest, tsc executable, persistent API transport, and thirteen-subpath export map: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F2.1 --aspect contract
  EXPECT: LEAF F2.1 CONTRACT PASS
  EVIDENCE: receipt=.outline/evidence/F2.1-contract.json sha256=90113b22651f011e2ac44625d8af688df8f9b28413359acc55e1f32b16c17f95

- [ ] G2: bamti package manifest, tsc executable, persistent API transport, and thirteen-subpath export map: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F2.1 --aspect evidence
  EXPECT: LEAF F2.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: bamti package manifest, tsc executable, persistent API transport, and thirteen-subpath export map: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F2.1 --aspect coverage
  EXPECT: LEAF F2.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: bamti package manifest, tsc executable, persistent API transport, and thirteen-subpath export map: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F2.1 --aspect regression
  EXPECT: LEAF F2.1 REGRESSION PASS
  EVIDENCE: pending

- [x] G5: bamti package manifest, tsc executable, persistent API transport, and thirteen-subpath export map: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F2.1 --aspect mutation
  EXPECT: LEAF F2.1 MUTATION PASS
  EVIDENCE: receipt=.outline/evidence/F2.1-mutation.json sha256=37e88f3a30eac09d35ce35457cd270726abc0911cc6e9c243bceb0fa22ac1754

- [ ] G6: bamti package manifest, tsc executable, persistent API transport, and thirteen-subpath export map: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate F2.1
  EXPECT: LEAF F2.1 PASS
  EVIDENCE: pending

## Scoped implementation evidence

- `cargo test -p bamts-cli --lib api_server` — 48 passed, 0 failed, 151 filtered.
- `api_server/{mod,wire,input,control,reader,session}.rs` replaces the deleted monolithic intake lifecycle. `TransportInput` owns the split reader/waker boundary; Unix stdin uses a `rustix::event::poll` self-pipe, TCP uses `shutdown(Read)`, the deterministic input is channel-backed, and the only non-reapable input is the Windows process-contained blocking stdin path.
- `Control` uses one FIFO plus bounded work bytes/count, bounded rejects, ticket-qualified cancellation, duplicate-live-ID rejection, reserved/deduplicated exit control, exact response retirement, and terminal flood error `-32006`. `MAX_QUEUED_BYTES` is a hard aggregate cap with no empty-queue bypass: a first request over 8 MiB receives answered `-32005`, accepted queued bytes never exceed the cap, and retirement returns both byte/work and response-obligation accounting to zero. IDs over 512 bytes receive `-32600` with `id: null`.
- `ReaderExitGuard` reports EOF, wake, read failure, and panic on every return/unwind path. Serving performs bounded reaping only after the exit report; no `Drop` implementation joins a reader. Output/log failures stop writes, wake/reap the reader, and retire the remaining obligations.
- Added lifecycle and invariant coverage includes >128-request saturation with exact ID accounting, first-request byte-cap rejection and exact live/queued accounting, exit behind saturation, EOF drain, live-ID reuse/rejection, oversize IDs, terminal flood accounting, repeated shutdown/exit reaping, read failure, write failure while input remains held, AST/service cancellation, and randomized-shape offer/cancel/retire conservation.
- Real process smoke: `cargo run -q -p bamts-cli --bin bamts -- --api` exited 0 after a framed `compiler/version` request plus `exit`, returning exactly one framed `bamts 0.2.0` response on stdout.
- `node --test npm/bamti/internal/transport.test.mjs` — 5 passed, including `-32005` -> `ServerBusyError` and `-32006` -> `TransportFloodError`; both are exported from JavaScript and declarations.
- `node --test npm/test/bamti-api.test.mjs` could not enter its seven assertions because all five required staged `npm/dist/bamti-cli-*-0.2.0.tgz` artifacts are absent. The unrelated F2.4 artifact precondition remains open; no F2.1 behavior failure was observed.
- The prescribed completion verifier, mutation runner, lint, format, and leaf gate were not run in this scoped concurrent rebuild, so G1-G6 remain unchecked.
