# Gates: C4.3 URI encoding family edge cases

Scope: C4.3 owns crates/bamts-runtime/src/builtins/uri.rs; it delivers URI encoding family edge cases.

- [ ] G1: URI encoding family edge cases: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.3 --aspect contract
  EXPECT: LEAF C4.3 CONTRACT PASS
  EVIDENCE: blocked: runtime-wave focused evidence recorded in the dated section below; no completion receipt was generated in this concurrent leaf pass.

- [ ] G2: URI encoding family edge cases: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.3 --aspect evidence
  EXPECT: LEAF C4.3 EVIDENCE PASS
  EVIDENCE: blocked: runtime-wave implementation/test evidence recorded in the dated section below; completion verification was not run in this concurrent leaf pass.

- [ ] G3: URI encoding family edge cases: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.3 --aspect coverage
  EXPECT: LEAF C4.3 COVERAGE PASS
  EVIDENCE: blocked: not run in this wave; deferred to the post-integration coverage/collection check described in the gate.

- [ ] G4: URI encoding family edge cases: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.3 --aspect regression
  EXPECT: LEAF C4.3 REGRESSION PASS
  EVIDENCE: blocked: not run in this wave; deferred to the post-integration regression check described in the gate.

- [ ] G5: URI encoding family edge cases: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.3 --aspect mutation
  EXPECT: LEAF C4.3 MUTATION PASS
  EVIDENCE: blocked: not run in this wave; deferred to the post-integration mutation check described in the gate.

- [ ] G6: URI encoding family edge cases: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C4.3
  EXPECT: LEAF C4.3 PASS
  EVIDENCE: blocked: not run in this wave; workspace build, lint, format, and final leaf gate remain the integration owner's check.

## Runtime numeric/text wave evidence (2026-08-26)

- OWNED SOURCE: `crates/bamts-runtime/src/builtins/uri.rs`
- FOCUSED CHECK: `cargo test -p bamts-runtime --lib builtins::uri::tests`
- RESULT: PASS — final post-integration rerun 10 passed, 0 failed, 933 filtered (artifact://14064).
- COVERAGE: encodeURI/component reserved-set split, uppercase UTF-8 escapes, paired/lone surrogate handling, reserved decode preservation, all UTF-8 width boundaries, and malformed percent/UTF-8 URIError classes.
- REMAINING EVIDENCE: G1-G6 remain unchecked because the completion contract/evidence/coverage/regression/mutation commands and `just leaf-gate` were not run in this concurrent leaf wave.

## Verification 2026-08-27 (post-966 restore)

Behavior-complete: `cargo test -p bamts-runtime --lib uri` 17/17 (includes
`builtins::uri::tests` plus substring matches in annex_b/array/collections/lib
tests); dedicated `builtins::uri::tests` is 10/10. Full runtime lib suite
966/966. G1-G6 stay pending (no receipt in `.outline/evidence/`; II.0.9).
Checkboxes unchanged.
