# Gates: B5.3 project references + composite + incremental (.tsbuildinfo)

Scope: B5.3 owns crates/bamts-compiler/src/project/references.rs; it delivers project references + composite + incremental (.tsbuildinfo).

- [ ] G1: project references + composite + incremental (.tsbuildinfo): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.3 --aspect contract
  EXPECT: LEAF B5.3 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: project references + composite + incremental (.tsbuildinfo): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.3 --aspect evidence
  EXPECT: LEAF B5.3 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: project references + composite + incremental (.tsbuildinfo): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.3 --aspect coverage
  EXPECT: LEAF B5.3 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: project references + composite + incremental (.tsbuildinfo): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.3 --aspect regression
  EXPECT: LEAF B5.3 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: project references + composite + incremental (.tsbuildinfo): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.3 --aspect mutation
  EXPECT: LEAF B5.3 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: project references + composite + incremental (.tsbuildinfo): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B5.3
  EXPECT: LEAF B5.3 PASS
  EVIDENCE: blocked: partial 2026-08-26 — cargo check -p bamts-compiler --lib PASS; cargo test -p bamts-compiler --lib project:: PASS (104 passed, 0 failed). Leaf lint/format and receipt aspects remain for the root gate.
