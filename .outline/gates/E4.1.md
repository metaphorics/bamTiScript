# Gates: E4.1 target-cell evidence schema + verification/toolchains/<triple>.json generator

Scope: E4.1 owns crates/bamts-verification/src/toolchain_schema.rs; it delivers target-cell evidence schema + verification/toolchains/<triple>.json generator.

- [ ] G1: target-cell evidence schema + verification/toolchains/<triple>.json generator: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E4.1 --aspect contract
  EXPECT: LEAF E4.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: target-cell evidence schema + verification/toolchains/<triple>.json generator: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E4.1 --aspect evidence
  EXPECT: LEAF E4.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: target-cell evidence schema + verification/toolchains/<triple>.json generator: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E4.1 --aspect coverage
  EXPECT: LEAF E4.1 COVERAGE PASS
  EVIDENCE: blocked: pending formal completion-verify; local toolchain evidence reports blocking=0 external_blocked=0 for all 28 obligations.

- [ ] G4: target-cell evidence schema + verification/toolchains/<triple>.json generator: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E4.1 --aspect regression
  EXPECT: LEAF E4.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: target-cell evidence schema + verification/toolchains/<triple>.json generator: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E4.1 --aspect mutation
  EXPECT: LEAF E4.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: target-cell evidence schema + verification/toolchains/<triple>.json generator: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate E4.1
  EXPECT: LEAF E4.1 PASS
  EVIDENCE: blocked: 2026-08-26 `cargo check -p bamts-verification --lib` and focused `toolchain_schema` lib tests passed; full leaf-gate not run (assignment: skip project-wide checks).
