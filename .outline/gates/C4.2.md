# Gates: C4.2 RegExp v-flag + unicode sets + duplicate named groups + lookbehind verification

Scope: C4.2 owns crates/bamts-runtime/src/builtins/regexp_v/; it delivers RegExp v-flag + unicode sets + duplicate named groups + lookbehind verification.

- [ ] G1: RegExp v-flag + unicode sets + duplicate named groups + lookbehind verification: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.2 --aspect contract
  EXPECT: LEAF C4.2 CONTRACT PASS
  EVIDENCE: blocked: runtime-wave focused evidence recorded in the dated section below; no completion receipt was generated in this concurrent leaf pass.

- [ ] G2: RegExp v-flag + unicode sets + duplicate named groups + lookbehind verification: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.2 --aspect evidence
  EXPECT: LEAF C4.2 EVIDENCE PASS
  EVIDENCE: blocked: runtime-wave implementation/test evidence recorded in the dated section below; completion verification was not run in this concurrent leaf pass.

- [ ] G3: RegExp v-flag + unicode sets + duplicate named groups + lookbehind verification: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.2 --aspect coverage
  EXPECT: LEAF C4.2 COVERAGE PASS
  EVIDENCE: blocked: not run in this wave; deferred to the post-integration coverage/collection check described in the gate.

- [ ] G4: RegExp v-flag + unicode sets + duplicate named groups + lookbehind verification: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.2 --aspect regression
  EXPECT: LEAF C4.2 REGRESSION PASS
  EVIDENCE: blocked: not run in this wave; deferred to the post-integration regression check described in the gate.

- [ ] G5: RegExp v-flag + unicode sets + duplicate named groups + lookbehind verification: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.2 --aspect mutation
  EXPECT: LEAF C4.2 MUTATION PASS
  EVIDENCE: blocked: not run in this wave; deferred to the post-integration mutation check described in the gate.

- [ ] G6: RegExp v-flag + unicode sets + duplicate named groups + lookbehind verification: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C4.2
  EXPECT: LEAF C4.2 PASS
  EVIDENCE: blocked: not run in this wave; workspace build, lint, format, and final leaf gate remain the integration owner's check.

## Runtime numeric/text wave evidence (2026-08-26)

- OWNED SOURCES: `crates/bamts-runtime/src/builtins/regexp_v/{mod.rs,unicode.rs,emoji_tables.rs}`
- FOCUSED CHECK: `cargo test -p bamts-runtime --lib builtins::regexp_v::tests`
- RESULT: PASS — central registration landed by integration owner; final rerun 30 passed, 0 failed, 913 filtered (artifact://14065).
- IMPLEMENTED: real v-mode matching/set algebra/properties, duplicate-name resolution, UTF-16 d-indices, stateful g/y `lastIndex`, dynamic RegExpExec override validation, CreateDataProperty result construction, and exact Unicode Table 66 `space` aliasing.
- REMAINING EVIDENCE: central dependency/module/baseline-RegExp/String/Annex-B wiring, then a nonzero focused-test run. G1-G6 remain unchecked; broad completion commands were not run.

## Verification 2026-08-27 (post-966 restore)

Behavior-complete: `cargo test -p bamts-runtime --lib regexp_v` 35/35;
full runtime lib suite 966/966. G1-G6 stay pending (no receipt in
`.outline/evidence/`; II.0.9). Checkboxes unchanged.
