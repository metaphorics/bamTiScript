# Gates: B0.1 AST/diagnostic change-window protocol (contract doc; syntax.rs/checker.rs edits batched per wave)

Scope: B0.1 owns docs/dev/ast_change_window_protocol.md; it delivers AST/diagnostic change-window protocol (contract doc; syntax.rs/checker.rs edits batched per wave).

- [x] G1: AST/diagnostic change-window protocol (contract doc; syntax.rs/checker.rs edits batched per wave): the declared public contract vectors pass
  CHECK: cargo run -q -p bamts-verification --locked -- completion evidence --leaf B0.1 --aspect contract
  EXPECT: receipt=.outline/evidence/B0.1-contract.json
  EVIDENCE: receipt=.outline/evidence/B0.1-contract.json sha256=9bee64c6c6583748da71f738ef54ec8259fe253794d23c75d910d571547f15df

- [ ] G2: AST/diagnostic change-window protocol (contract doc; syntax.rs/checker.rs edits batched per wave): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B0.1 --aspect evidence
  EXPECT: LEAF B0.1 EVIDENCE PASS
  EVIDENCE: blocked: blocked on program-wide completeness ledger: proof/completeness-ledger.json manifest digest is stale against verification/manifest.lock.json (StaleEvidence); typescript-7.0.2 has 12782 BLOCKING_FAIL rows owned by P1 and zero PASS rows in the ledger. Not B0.1-scope.

- [ ] G3: AST/diagnostic change-window protocol (contract doc; syntax.rs/checker.rs edits batched per wave): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B0.1 --aspect coverage
  EXPECT: LEAF B0.1 COVERAGE PASS
  EVIDENCE: blocked: blocked on program-wide completeness ledger: proof/completeness-ledger.json manifest digest is stale against verification/manifest.lock.json (StaleEvidence); typescript-7.0.2 has 12782 BLOCKING_FAIL rows owned by P1 and zero PASS rows in the ledger. Not B0.1-scope.

- [ ] G4: AST/diagnostic change-window protocol (contract doc; syntax.rs/checker.rs edits batched per wave): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B0.1 --aspect regression
  EXPECT: LEAF B0.1 REGRESSION PASS
  EVIDENCE: blocked: blocked on program-wide completeness ledger: proof/completeness-ledger.json manifest digest is stale against verification/manifest.lock.json (StaleEvidence); typescript-7.0.2 has 12782 BLOCKING_FAIL rows owned by P1 and zero PASS rows in the ledger. Not B0.1-scope.

- [x] G5: AST/diagnostic change-window protocol (contract doc; syntax.rs/checker.rs edits batched per wave): the registered mutation makes its contract gate fail
  CHECK: cargo run -q -p bamts-verification --locked -- completion evidence --leaf B0.1 --aspect mutation
  EXPECT: receipt=.outline/evidence/B0.1-mutation.json
  EVIDENCE: receipt=.outline/evidence/B0.1-mutation.json sha256=443474adb5161c16f44852af9b24e5b87b0741f99022a2dba1c867a1bb7bf425

- [ ] G6: AST/diagnostic change-window protocol (contract doc; syntax.rs/checker.rs edits batched per wave): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: cargo check --offline -p bamts-compiler ; cargo test --offline -p bamts-compiler --lib
  EXPECT: LEAF B0.1 PASS
  EVIDENCE: blocked: cargo check --offline -p bamts-compiler finished. cargo test --offline -p bamts-compiler --lib: 1520 passed, 2 failed (pathe corpus MissingExpression in lower.rs/program.rs; not the protocol doc); leaf-gate rerun against the repository Justfile pending.
