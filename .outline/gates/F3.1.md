# Gates: F3.1 completion runbook + docs + changelog

Scope: F3.1 owns docs/release/runbook.md; it delivers completion runbook + docs + changelog.

- [ ] G1: completion runbook + docs + changelog: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F3.1 --aspect contract
  EXPECT: LEAF F3.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: completion runbook + docs + changelog: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F3.1 --aspect evidence
  EXPECT: LEAF F3.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: completion runbook + docs + changelog: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F3.1 --aspect coverage
  EXPECT: LEAF F3.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: completion runbook + docs + changelog: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F3.1 --aspect regression
  EXPECT: LEAF F3.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: completion runbook + docs + changelog: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf F3.1 --aspect mutation
  EXPECT: LEAF F3.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: completion runbook + docs + changelog: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate F3.1
  EXPECT: LEAF F3.1 PASS
  EVIDENCE: pending
