# Gates: C7.2 GC stress hardening (ephemeron edge, watermark tuning under lane)

Scope: C7.2 owns crates/bamts-runtime/src/gc/stress_hardening.rs; it delivers GC stress hardening (ephemeron edge, watermark tuning under lane).

- [ ] G1: GC stress hardening (ephemeron edge, watermark tuning under lane): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C7.2 --aspect contract
  EXPECT: LEAF C7.2 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: GC stress hardening (ephemeron edge, watermark tuning under lane): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C7.2 --aspect evidence
  EXPECT: LEAF C7.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: GC stress hardening (ephemeron edge, watermark tuning under lane): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C7.2 --aspect coverage
  EXPECT: LEAF C7.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: GC stress hardening (ephemeron edge, watermark tuning under lane): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C7.2 --aspect regression
  EXPECT: LEAF C7.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: GC stress hardening (ephemeron edge, watermark tuning under lane): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C7.2 --aspect mutation
  EXPECT: LEAF C7.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: GC stress hardening (ephemeron edge, watermark tuning under lane): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C7.2
  EXPECT: LEAF C7.2 PASS
  EVIDENCE: pending

## Runtime GC/data wave evidence — 2026-08-26

- The collector's ordinary trace drains before a WeakMap ephemeron fixed point; newly reachable values can expose another weak collection and force another round before weak entries are purged.
- Post-collection watermarks are recomputed from retained bytes and live runtime slots with saturating multiplication and configured-limit caps. The allocation-pressure test also proves the byte ledger equals machine bytes plus all slot charges after collection.
- cargo test -p bamts-runtime --lib stress_hardening: **6 passed, 0 failed, 796 filtered** (artifact://12150). Coverage includes a two-WeakMap fixed-point chain, composed heap edges, cleanup retry, exactly-one-job WeakRef retention, pressure/watermark recomputation, and exact weak-collection index/byte refunds.
- The formal G1–G6 checkboxes above remain pending: no completion verifier, mutation campaign, formatter, linter, or broad leaf gate ran in this concurrent wave.

## Verification 2026-08-27 (post-966 restore)

Behavior-complete: `cargo test -p bamts-runtime --lib stress_hardening` 6/6;
full runtime lib suite 966/966. G1-G6 stay pending (no receipt in
`.outline/evidence/`; II.0.9). Checkboxes unchanged.
