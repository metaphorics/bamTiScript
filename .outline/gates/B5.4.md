# Gates: B5.4 build mode (tsc -b) parity

Scope: B5.4 owns crates/bamts-compiler/src/project/build_mode.rs; it delivers build mode (tsc -b) parity.

- [ ] G1: build mode (tsc -b) parity: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.4 --aspect contract
  EXPECT: LEAF B5.4 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: build mode (tsc -b) parity: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.4 --aspect evidence
  EXPECT: LEAF B5.4 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: build mode (tsc -b) parity: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.4 --aspect coverage
  EXPECT: LEAF B5.4 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: build mode (tsc -b) parity: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.4 --aspect regression
  EXPECT: LEAF B5.4 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: build mode (tsc -b) parity: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.4 --aspect mutation
  EXPECT: LEAF B5.4 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: build mode (tsc -b) parity: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B5.4
  EXPECT: LEAF B5.4 PASS
  EVIDENCE: blocked: partial 2026-08-26 — cargo check -p bamts-compiler --lib PASS; cargo test -p bamts-compiler --lib project:: PASS (104 passed, 0 failed). Leaf lint/format and receipt aspects remain for the root gate.
