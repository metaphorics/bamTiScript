# Gates: C2.3 property descriptor algebra edge cases + prototype invariants

Scope: C2.3 owns crates/bamts-runtime/src/builtins/property_descriptor.rs; it delivers property descriptor algebra edge cases + prototype invariants.

- [ ] G1: property descriptor algebra edge cases + prototype invariants: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C2.3 --aspect contract
  EXPECT: LEAF C2.3 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: property descriptor algebra edge cases + prototype invariants: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C2.3 --aspect evidence
  EXPECT: LEAF C2.3 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: property descriptor algebra edge cases + prototype invariants: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C2.3 --aspect coverage
  EXPECT: LEAF C2.3 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: property descriptor algebra edge cases + prototype invariants: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C2.3 --aspect regression
  EXPECT: LEAF C2.3 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: property descriptor algebra edge cases + prototype invariants: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C2.3 --aspect mutation
  EXPECT: LEAF C2.3 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: property descriptor algebra edge cases + prototype invariants: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C2.3
  EXPECT: LEAF C2.3 PASS
  EVIDENCE: pending

## Verification 2026-08-27 (post-966 restore)

Behavior-complete: `cargo test -p bamts-runtime --lib property_descriptor`
passes 10/10; full runtime lib suite 966/966. Gates remain pending: no
content-addressed receipt exists in `.outline/evidence/` and the trusted
leaf-evidence runner is unavailable, so G2 cannot be satisfied; closure is
deferred to II.0.9. No receipt fabricated; checkboxes unchanged.
