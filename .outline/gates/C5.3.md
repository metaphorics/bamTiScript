# Gates: C5.3 explicit resource management runtime (using/await using disposal, DisposableStack)

Scope: C5.3 owns crates/bamts-runtime/src/vm/explicit_resource.rs; it delivers explicit resource management runtime (using/await using disposal, DisposableStack).

- [ ] G1: explicit resource management runtime (using/await using disposal, DisposableStack): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.3 --aspect contract
  EXPECT: LEAF C5.3 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: explicit resource management runtime (using/await using disposal, DisposableStack): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.3 --aspect evidence
  EXPECT: LEAF C5.3 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: explicit resource management runtime (using/await using disposal, DisposableStack): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.3 --aspect coverage
  EXPECT: LEAF C5.3 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: explicit resource management runtime (using/await using disposal, DisposableStack): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.3 --aspect regression
  EXPECT: LEAF C5.3 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: explicit resource management runtime (using/await using disposal, DisposableStack): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.3 --aspect mutation
  EXPECT: LEAF C5.3 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: explicit resource management runtime (using/await using disposal, DisposableStack): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.3 --aspect regression
  EXPECT: LEAF C5.3 REGRESSION PASS
  EVIDENCE: pending

## Verification 2026-08-27 (post-966 restore)

Behavior-complete: `cargo test -p bamts-runtime --lib explicit_resource` 22/22;
full runtime lib suite 966/966. G1-G6 stay pending (no receipt in
`.outline/evidence/`; II.0.9). Checkboxes unchanged.
