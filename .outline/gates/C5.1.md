# Gates: C5.1 generator + async function gap closure

Scope: C5.1 owns crates/bamts-runtime/src/vm/generator_async.rs; it delivers generator + async function gap closure.

- [ ] G1: generator + async function gap closure: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.1 --aspect contract
  EXPECT: LEAF C5.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: generator + async function gap closure: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.1 --aspect evidence
  EXPECT: LEAF C5.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: generator + async function gap closure: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.1 --aspect coverage
  EXPECT: LEAF C5.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: generator + async function gap closure: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.1 --aspect regression
  EXPECT: LEAF C5.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: generator + async function gap closure: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.1 --aspect mutation
  EXPECT: LEAF C5.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: generator + async function gap closure: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C5.1 --aspect regression
  EXPECT: LEAF C5.1 REGRESSION PASS
  EVIDENCE: pending

## Verification 2026-08-27 (post-966 restore)

Behavior-complete: `cargo test -p bamts-runtime --lib generator_async` 10/10;
full runtime lib suite 966/966. G1-G6 stay pending (no receipt in
`.outline/evidence/`; II.0.9). Checkboxes unchanged.
