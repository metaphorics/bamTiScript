# Gates: B3.4 generic inference depth (priorities, contextual typing)

Scope: B3.4 owns crates/bamts-compiler/src/checker/inference.rs; it delivers generic inference depth (priorities, contextual typing).

- [ ] G1: generic inference depth (priorities, contextual typing): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.4 --aspect contract
  EXPECT: LEAF B3.4 CONTRACT PASS
  EVIDENCE: blocked: pre-integration isolated-checkout proof: `cargo test -p bamts-compiler --lib -- checker::inference::` passed 26/26 tests; the completion verifier remains integrator-owned and this gate remains unchecked.

- [ ] G2: generic inference depth (priorities, contextual typing): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.4 --aspect evidence
  EXPECT: LEAF B3.4 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: generic inference depth (priorities, contextual typing): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.4 --aspect coverage
  EXPECT: LEAF B3.4 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: generic inference depth (priorities, contextual typing): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.4 --aspect regression
  EXPECT: LEAF B3.4 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: generic inference depth (priorities, contextual typing): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.4 --aspect mutation
  EXPECT: LEAF B3.4 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: generic inference depth (priorities, contextual typing): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B3.4
  EXPECT: LEAF B3.4 PASS
  EVIDENCE: blocked: UNMET: canonical `just leaf-gate B3.4` runner is absent. Scoped proof on 2026-08-26: inference tests passed 21/21, `cargo check -p bamts-compiler --lib` passed, and integrated checker tests passed 739/739. G1-G5 remain unchecked; no completion receipts were fabricated.
