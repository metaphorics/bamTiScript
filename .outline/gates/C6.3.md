# Gates: C6.3 dynamic import + host hook contract

Scope: C6.3 owns crates/bamts-runtime/src/vm/dynamic_import.rs; it delivers dynamic import + host hook contract.

- [ ] G1: dynamic import + host hook contract: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.3 --aspect contract
  EXPECT: LEAF C6.3 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: dynamic import + host hook contract: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.3 --aspect evidence
  EXPECT: LEAF C6.3 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: dynamic import + host hook contract: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.3 --aspect coverage
  EXPECT: LEAF C6.3 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: dynamic import + host hook contract: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.3 --aspect regression
  EXPECT: LEAF C6.3 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: dynamic import + host hook contract: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.3 --aspect mutation
  EXPECT: LEAF C6.3 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: dynamic import + host hook contract: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C6.3
  EXPECT: LEAF C6.3 PASS
  EVIDENCE: pending
