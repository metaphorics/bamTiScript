# Gates: C11.2 PlainDate/PlainDateTime/PlainYearMonth/PlainMonthDay + ISO calendar

Scope: C11.2 owns crates/bamts-runtime/src/temporal/plain_types.rs; it delivers PlainDate/PlainDateTime/PlainYearMonth/PlainMonthDay + ISO calendar.

- [ ] G1: PlainDate/PlainDateTime/PlainYearMonth/PlainMonthDay + ISO calendar: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.2 --aspect contract
  EXPECT: LEAF C11.2 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: PlainDate/PlainDateTime/PlainYearMonth/PlainMonthDay + ISO calendar: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.2 --aspect evidence
  EXPECT: LEAF C11.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: PlainDate/PlainDateTime/PlainYearMonth/PlainMonthDay + ISO calendar: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.2 --aspect coverage
  EXPECT: LEAF C11.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: PlainDate/PlainDateTime/PlainYearMonth/PlainMonthDay + ISO calendar: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.2 --aspect regression
  EXPECT: LEAF C11.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: PlainDate/PlainDateTime/PlainYearMonth/PlainMonthDay + ISO calendar: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.2 --aspect mutation
  EXPECT: LEAF C11.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: PlainDate/PlainDateTime/PlainYearMonth/PlainMonthDay + ISO calendar: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C11.2
  EXPECT: LEAF C11.2 PASS
  EVIDENCE: pending
