# Gates: B4.1 JS emit parity incl. target downlevels (async/generator/class-fields/destructuring transforms)

Scope: B4.1 owns crates/bamts-compiler/src/emitter/transforms.rs; it delivers JS emit parity incl. target downlevels (async/generator/class-fields/destructuring transforms).

- [ ] G1: JS emit parity incl. target downlevels (async/generator/class-fields/destructuring transforms): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.1 --aspect contract
  EXPECT: LEAF B4.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: JS emit parity incl. target downlevels (async/generator/class-fields/destructuring transforms): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.1 --aspect evidence
  EXPECT: LEAF B4.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: JS emit parity incl. target downlevels (async/generator/class-fields/destructuring transforms): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.1 --aspect coverage
  EXPECT: LEAF B4.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: JS emit parity incl. target downlevels (async/generator/class-fields/destructuring transforms): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.1 --aspect regression
  EXPECT: LEAF B4.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: JS emit parity incl. target downlevels (async/generator/class-fields/destructuring transforms): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.1 --aspect mutation
  EXPECT: LEAF B4.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: JS emit parity incl. target downlevels (async/generator/class-fields/destructuring transforms): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B4.1
  EXPECT: LEAF B4.1 PASS
  EVIDENCE: blocked: partial 2026-08-26 — replaced the ad-hoc instance/static/computed/member collectors in `emitter/transforms.rs` with ordered `FieldMode`/`FieldInit`/`StaticStep`/`ClassLowering` state, recursive statement/expression traversal, recursive post-successful-`super()` insertion, class-expression lowering, define/assign/native modes, declare/abstract omission, anonymous default-name restoration before statics, static-block interleaving, and scope-aware static `this`/`super` rebinding. Moved static-block traversal now rewrites blocks, branches, loops, switches, try/catch/finally, labels, throws/returns, and variable initializers while stopping at nested non-arrow function/class scopes. Static simple/computed/compound superclass writes lower through ordered single-evaluation arrow IIFEs using `Reflect.get`/`Reflect.set` with the derived constructor receiver; the emitted code contains no out-of-class `super`. `cargo test -p bamts-compiler --lib emitter::transforms::tests:: -- --nocapture` PASS (30 passed, 0 failed before adding the executed regression); `cargo test -p bamts-compiler --lib emitter:: -- --nocapture` PASS (121 passed, 0 failed); `cargo test -p bamts-compiler --lib emitter::transforms::tests::node_executes_nested_static_super_writes -- --nocapture` PASS (1 passed, Node v26.7.0 local compatibility proof); `BAMTS_ALLOW_NODE_COMPAT=1 cargo test -p bamts-verification --test class_lowering_order -- --nocapture` PASS (3 passed, 0 failed; cases: heritage/key/static order and single evaluation, recursive super plus nested static this/super writes, default-name forwarding and abrupt super completion; native Node vs ES2015 lowered stdout matched the expected literals). Strict default remains Node 24 (`assert!(version.starts_with("v24."))` unless `BAMTS_ALLOW_NODE_COMPAT` is set). `cargo check -p bamts-compiler` PASS. Mutation guards are the static-order/single-evaluation behavior case (kills reversed `StaticStep` order and computed-key re-evaluation) and recursive-super behavior case (kills pre-`super()` field initialization). Leaf lint/format, strict pinned Node 24 execution, and receipt aspects remain for the root gate.
