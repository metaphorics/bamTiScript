# Gates: C3.4 ArrayBuffer resize/transfer/maxByteLength

Scope: C3.4 owns crates/bamts-runtime/src/builtins/arraybuffer.rs; it delivers ArrayBuffer resize/transfer/maxByteLength.

- [ ] G1: ArrayBuffer resize/transfer/maxByteLength: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.4 --aspect contract
  EXPECT: LEAF C3.4 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: ArrayBuffer resize/transfer/maxByteLength: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.4 --aspect evidence
  EXPECT: LEAF C3.4 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: ArrayBuffer resize/transfer/maxByteLength: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.4 --aspect coverage
  EXPECT: LEAF C3.4 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: ArrayBuffer resize/transfer/maxByteLength: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.4 --aspect regression
  EXPECT: LEAF C3.4 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: ArrayBuffer resize/transfer/maxByteLength: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.4 --aspect mutation
  EXPECT: LEAF C3.4 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: ArrayBuffer resize/transfer/maxByteLength: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C3.4
  EXPECT: LEAF C3.4 PASS
  EVIDENCE: pending

## 2026-08-26 runtime-collections leaf wave

- SOURCE: `arraybuffer.rs` retains resizable storage, resize/slice/transfer/transferToFixedLength, detach-key, allocation-failure atomicity, and shared-block primitives. This wave fixed detached resizable identity and moved `byteLength > maxByteLength` validation before observable `newTarget.prototype` lookup.
- FOCUSED CHECK: `cargo test -p bamts-runtime builtins::arraybuffer::tests -- --nocapture`.
- OBSERVED: **not verified**. The latest run exited 101 in unrelated `builtins/annex_b.rs:49` before leaf compilation (`artifact://12080`); the module is orphaned, so a later nonzero run is mandatory.
- WIRING: requires module/installer registration, ArrayBuffer constructor/prototype caches, `HeapEntry::ArrayBuffer`, host detach routing, ArrayBuffer.isView/DataView completion, and exhaustive GC/native/property/accounting arms.
