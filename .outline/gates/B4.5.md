# Gates: B4.5 transpile-mode parity (transpileRunner 22 cases; single-file API)

Scope: B4.5 owns crates/bamts-compiler/src/emitter/transpile.rs; it delivers transpile-mode parity (transpileRunner 22 cases; single-file API).

- [ ] G1: transpile-mode parity (transpileRunner 22 cases; single-file API): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.5 --aspect contract
  EXPECT: LEAF B4.5 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: transpile-mode parity (transpileRunner 22 cases; single-file API): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.5 --aspect evidence
  EXPECT: LEAF B4.5 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: transpile-mode parity (transpileRunner 22 cases; single-file API): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.5 --aspect coverage
  EXPECT: LEAF B4.5 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: transpile-mode parity (transpileRunner 22 cases; single-file API): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.5 --aspect regression
  EXPECT: LEAF B4.5 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: transpile-mode parity (transpileRunner 22 cases; single-file API): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.5 --aspect mutation
  EXPECT: LEAF B4.5 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: transpile-mode parity (transpileRunner 22 cases; single-file API): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B4.5
  EXPECT: LEAF B4.5 PASS
  EVIDENCE: blocked: partial 2026-08-26 — cargo check -p bamts-compiler --lib PASS; cargo test -p bamts-compiler --lib emitter:: PASS (111 passed, 0 failed). Leaf lint/format and receipt aspects remain for the root gate.
