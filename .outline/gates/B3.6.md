# Gates: B3.6 JSX checking parity

Scope: B3.6 owns crates/bamts-compiler/src/checker/jsx.rs; it delivers JSX checking parity.

- [ ] G1: JSX checking parity: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.6 --aspect contract
  EXPECT: LEAF B3.6 CONTRACT PASS
  EVIDENCE: blocked: isolated checker snapshot, `cargo test -p bamts-compiler --lib checker::jsx::tests`: 26 passed; prescribed completion verifier unavailable

- [ ] G2: JSX checking parity: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.6 --aspect evidence
  EXPECT: LEAF B3.6 EVIDENCE PASS
  EVIDENCE: blocked: pending; no B3.6 receipt registry is present

- [ ] G3: JSX checking parity: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.6 --aspect coverage
  EXPECT: LEAF B3.6 COVERAGE PASS
  EVIDENCE: blocked: pending; prescribed completion verifier unavailable

- [ ] G4: JSX checking parity: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.6 --aspect regression
  EXPECT: LEAF B3.6 REGRESSION PASS
  EVIDENCE: blocked: isolated full JSX module suite remained 26/26 after restoring the mutation; prescribed completion verifier unavailable

- [ ] G5: JSX checking parity: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.6 --aspect mutation
  EXPECT: LEAF B3.6 MUTATION PASS
  EVIDENCE: blocked: isolated mutation changed lowercase JSX classification to value-based; full module suite failed 6 tests, including the direct classification contract

- [ ] G6: JSX checking parity: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B3.6
  EXPECT: LEAF B3.6 PASS
  EVIDENCE: blocked: UNMET: canonical `just leaf-gate B3.6` runner is absent. Scoped proof on 2026-08-26: JSX tests passed 28/28, `cargo check -p bamts-compiler --lib` passed, and integrated checker tests passed 739/739. G1-G5 remain unchecked without current completion receipts; the recorded mutation evidence above is retained.
