# Gates: C10.4 DateTimeFormat

Scope: C10.4 owns crates/bamts-runtime/src/intl/calendar.rs; crates/bamts-runtime/src/intl/time_zone.rs; crates/bamts-runtime/src/intl/temporal_input.rs; crates/bamts-runtime/src/intl/icu_adapter/mod.rs; crates/bamts-runtime/src/intl/icu_adapter/date_time.rs; crates/bamts-runtime/src/intl/date_time_format.rs; crates/bamts-runtime/src/builtins/intl/date_time_format.rs; it delivers DateTimeFormat.

- [ ] G1: DateTimeFormat: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.4 --aspect contract
  EXPECT: LEAF C10.4 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: DateTimeFormat: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.4 --aspect evidence
  EXPECT: LEAF C10.4 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: DateTimeFormat: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.4 --aspect coverage
  EXPECT: LEAF C10.4 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: DateTimeFormat: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.4 --aspect regression
  EXPECT: LEAF C10.4 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: DateTimeFormat: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.4 --aspect mutation
  EXPECT: LEAF C10.4 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: DateTimeFormat: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C10.4
  EXPECT: LEAF C10.4 PASS
  EVIDENCE: pending
