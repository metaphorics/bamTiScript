# Gates: B6.2 diagnostic rendering parity (pretty false canonical text; json)

Scope: B6.2 owns crates/bamts-cli/src/cli/diagnostic_format.rs; it delivers diagnostic rendering parity (pretty false canonical text; json).

- [x] G1: diagnostic rendering parity (pretty false canonical text; json): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B6.2 --aspect contract
  EXPECT: LEAF B6.2 CONTRACT PASS
  EVIDENCE: receipt=.outline/evidence/B6.2-contract.json sha256=564e1a408216066eb8b7e3750c379e19245a85d31b8beb53157a8ec7b64aef9f
  Contract detail: canonical write at diagnostic_format.rs:162 is path(line,col): with trailing space; utf16_columns_for_non_bmp and pretty_false exact-text tests remain. cargo test -p bamts-cli --lib diagnostic_format did not compile (bamts-codegen E0080/E0004 outside owns).

- [ ] G2: diagnostic rendering parity (pretty false canonical text; json): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B6.2 --aspect evidence
  EXPECT: LEAF B6.2 EVIDENCE PASS
  EVIDENCE: blocked: blocked on program-wide completeness ledger: proof/completeness-ledger.json manifest digest is stale against verification/manifest.lock.json (StaleEvidence); typescript-7.0.2 has 12782 BLOCKING_FAIL rows owned by P1 and zero PASS rows in the ledger. Not B6-scope.

- [ ] G3: diagnostic rendering parity (pretty false canonical text; json): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B6.2 --aspect coverage
  EXPECT: LEAF B6.2 COVERAGE PASS
  EVIDENCE: blocked: blocked on program-wide completeness ledger: proof/completeness-ledger.json manifest digest is stale against verification/manifest.lock.json (StaleEvidence); typescript-7.0.2 has 12782 BLOCKING_FAIL rows owned by P1 and zero PASS rows in the ledger. Not B6-scope.

- [ ] G4: diagnostic rendering parity (pretty false canonical text; json): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B6.2 --aspect regression
  EXPECT: LEAF B6.2 REGRESSION PASS
  EVIDENCE: blocked: blocked on program-wide completeness ledger: proof/completeness-ledger.json manifest digest is stale against verification/manifest.lock.json (StaleEvidence); typescript-7.0.2 has 12782 BLOCKING_FAIL rows owned by P1 and zero PASS rows in the ledger. Not B6-scope.

- [ ] G5: diagnostic rendering parity (pretty false canonical text; json): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B6.2 --aspect mutation
  EXPECT: LEAF B6.2 MUTATION PASS
  EVIDENCE: blocked: mutation receipt recorded at .outline/evidence/B6.2-mutation.json; deleted the trailing space after the close paren; guard tests 372/385/398/527 were not reached: cargo test failed compiling bamts-codegen. Box left open until those guards fail under the mutation.

- [ ] G6: diagnostic rendering parity (pretty false canonical text; json): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: cargo fmt -p bamts-cli -- --check ; cargo check --offline -p bamts-cli ; cargo check --offline -p bamts-compiler ; cargo clippy --offline -p bamts-cli --all-targets -- -D warnings ; cargo test --offline -p bamts-cli --lib -- diagnostic_format
  EXPECT: LEAF B6.2 PASS
  EVIDENCE: blocked: cargo check --offline -p bamts-compiler finished. cargo check --offline -p bamts-cli and cargo test --offline -p bamts-cli --lib -- diagnostic_format fail compiling bamts-codegen (E0080/E0004); leaf-gate rerun pending.

## Workstream evidence (not gate closure)

- Authority: `/tmp/bamts-tsc-7.0.2`, built from pinned checkout commit `1e4744d68260a7cb91b62b12edc3f6a2187faaf1`, reported `Version 7.0.2`.
- Canonical pretty-false renderer still emits `path(line,col): ` at diagnostic_format.rs:162.
- Focused renderer tests remain for exact canonical lines, UTF-16 columns, JSON typing, no hidden cap.
- `cargo test -p bamts-cli --lib -- diagnostic_format` does not reach bamts-cli: concurrent bamts-codegen opcode/helper integration fails compilation outside B6 ownership.
