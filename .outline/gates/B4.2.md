# Gates: B4.2 .d.ts emit parity

Scope: B4.2 owns crates/bamts-compiler/src/emitter/declarations.rs; it delivers .d.ts emit parity.

- [ ] G1: .d.ts emit parity: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.2 --aspect contract
  EXPECT: LEAF B4.2 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: .d.ts emit parity: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.2 --aspect evidence
  EXPECT: LEAF B4.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: .d.ts emit parity: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.2 --aspect coverage
  EXPECT: LEAF B4.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: .d.ts emit parity: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.2 --aspect regression
  EXPECT: LEAF B4.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: .d.ts emit parity: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B4.2 --aspect mutation
  EXPECT: LEAF B4.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: .d.ts emit parity: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B4.2
  EXPECT: LEAF B4.2 PASS
  EVIDENCE: blocked: partial 2026-08-26 — cargo check -p bamts-compiler --lib PASS; cargo test -p bamts-compiler --lib emitter:: PASS (111 passed, 0 failed). Leaf lint/format and receipt aspects remain for the root gate.
