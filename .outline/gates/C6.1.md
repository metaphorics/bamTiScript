# Gates: C6.1 ESM evaluation semantics (cycles, live bindings, TLA) in vm/program

Scope: C6.1 owns crates/bamts-runtime/src/vm/esm_eval.rs; it delivers ESM evaluation semantics (cycles, live bindings, TLA) in vm/program.

- [ ] G1: ESM evaluation semantics (cycles, live bindings, TLA) in vm/program: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.1 --aspect contract
  EXPECT: LEAF C6.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: ESM evaluation semantics (cycles, live bindings, TLA) in vm/program: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.1 --aspect evidence
  EXPECT: LEAF C6.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: ESM evaluation semantics (cycles, live bindings, TLA) in vm/program: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.1 --aspect coverage
  EXPECT: LEAF C6.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: ESM evaluation semantics (cycles, live bindings, TLA) in vm/program: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.1 --aspect regression
  EXPECT: LEAF C6.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: ESM evaluation semantics (cycles, live bindings, TLA) in vm/program: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.1 --aspect mutation
  EXPECT: LEAF C6.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: ESM evaluation semantics (cycles, live bindings, TLA) in vm/program: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C6.1
  EXPECT: LEAF C6.1 PASS
  EVIDENCE: pending
