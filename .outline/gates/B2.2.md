# Gates: B2.2 export/symbol-flag parity (visibility, re-export chains)

Scope: B2.2 owns crates/bamts-compiler/src/binder/exports.rs; it delivers export/symbol-flag parity (visibility, re-export chains).

- [ ] G1: export/symbol-flag parity (visibility, re-export chains): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B2.2 --aspect contract
  EXPECT: LEAF B2.2 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: export/symbol-flag parity (visibility, re-export chains): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B2.2 --aspect evidence
  EXPECT: LEAF B2.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: export/symbol-flag parity (visibility, re-export chains): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B2.2 --aspect coverage
  EXPECT: LEAF B2.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: export/symbol-flag parity (visibility, re-export chains): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B2.2 --aspect regression
  EXPECT: LEAF B2.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: export/symbol-flag parity (visibility, re-export chains): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B2.2 --aspect mutation
  EXPECT: LEAF B2.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: export/symbol-flag parity (visibility, re-export chains): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B2.2
  EXPECT: LEAF B2.2 PASS
  EVIDENCE: pending

## Current scoped repair evidence (2026-08-26)

- The export table tracks every exported element, not only value-visible exports.
- The production checker binder's source scan now applies the same TS2309-equivalent rule to every non-assignment export declaration, including `export type { X }`, `export { type X }`, and `export type *`.
- `export =` therefore conflicts with type-only exports as well as value, default, namespace, and value-star exports in either declaration order.
- The model's assignment anchor and once-per-module latch keep `MIXED_EXPORT_ASSIGNMENT` deterministic; the production scan emits once at each assignment range after observing any other exported element.
- Model regressions cover type-only named/star exports in both orders and assert one anchored diagnostic. Source-level regressions cover all three type-only spellings in both orders and replace the former allow fixtures.
- `cargo test -p bamts-compiler export_assignment_rejects_type_only_exports_in_both_orders`: PASS.
- `cargo test -p bamts-compiler exports`: PASS (54 tests across 4 suites).
- `cargo check -p bamts-compiler`: PASS, with one pre-existing unrelated `service::build_snapshot` dead-code warning.
- Diagnostic catalog follow-up: the repaired TS2309-equivalent path uses `MIXED_EXPORT_ASSIGNMENT` (`BAMTS-C017`). This file also defines `EXPORT_CYCLE` (`BAMTS-C022`) and `EXPORT_AMBIGUOUS` (`BAMTS-C023`); codes `BAMTS-C021`–`BAMTS-C024` collide with checker codes and require coordinated catalog reassignment. This repair does not independently invent replacement codes.

G1–G6 remain unchecked because their declared completion-framework, receipt, coverage, regression, mutation, and full leaf-gate commands were not run by this scoped repair.
