# Gates: E1.1 bytecode ISA gap closures (lane-driven opcode/operand additions)

Scope: E1.1 owns crates/bamts-bytecode/src/isa.rs and the root Instruction/codec integration in crates/bamts-bytecode/src/lib.rs; it delivers append-only bytecode ISA gap closures required by live lowering contracts.

- [ ] G1: bytecode ISA gap closures (lane-driven opcode/operand additions): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E1.1 --aspect contract
  EXPECT: LEAF E1.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: bytecode ISA gap closures (lane-driven opcode/operand additions): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E1.1 --aspect evidence
  EXPECT: LEAF E1.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: bytecode ISA gap closures (lane-driven opcode/operand additions): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E1.1 --aspect coverage
  EXPECT: LEAF E1.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: bytecode ISA gap closures (lane-driven opcode/operand additions): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E1.1 --aspect regression
  EXPECT: LEAF E1.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: bytecode ISA gap closures (lane-driven opcode/operand additions): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E1.1 --aspect mutation
  EXPECT: LEAF E1.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: bytecode ISA gap closures (lane-driven opcode/operand additions): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate E1.1
  EXPECT: LEAF E1.1 PASS
  EVIDENCE: pending

Focused implementation evidence (2026-08-26):
- Root Instruction and wire codec now expose append-only tags 52..=57: GetSuper, SetSuper, ImportAttributes, ImportDynamicAttributes, CopyDataProperties, and GetTemplateObject. Existing tags 0..=51 are unchanged.
- cargo test -p bamts-bytecode --lib: PASS, 132 passed, 0 failed.
- cargo check -p bamts-bytecode --lib: PASS.
- cargo clippy -p bamts-bytecode --all-targets -- -D warnings: PASS.
- cargo fmt -p bamts-bytecode -- --check: PASS.
- G1-G6 remain unchecked because their exact completion/receipt/mutation/leaf-gate commands were not run in this shared-tree wave; focused crate evidence does not substitute for those receipts.
