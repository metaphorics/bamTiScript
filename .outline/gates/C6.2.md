# Gates: C6.2 import attributes + JSON modules

Scope: C6.2 owns crates/bamts-runtime/src/vm/import_attributes.rs; it delivers import attributes + JSON modules.

- [ ] G1: import attributes + JSON modules: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.2 --aspect contract
  EXPECT: LEAF C6.2 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: import attributes + JSON modules: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.2 --aspect evidence
  EXPECT: LEAF C6.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: import attributes + JSON modules: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.2 --aspect coverage
  EXPECT: LEAF C6.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: import attributes + JSON modules: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.2 --aspect regression
  EXPECT: LEAF C6.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: import attributes + JSON modules: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C6.2 --aspect mutation
  EXPECT: LEAF C6.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: import attributes + JSON modules: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C6.2
  EXPECT: LEAF C6.2 PASS
  EVIDENCE: pending
