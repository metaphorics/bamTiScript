# Gates: E4.3 aarch64 cell (cross + QEMU)

Scope: E4.3 owns verification/toolchains/aarch64-unknown-linux-gnu.json; it delivers aarch64 cell (cross + QEMU).

- [ ] G1: aarch64 cell (cross + QEMU): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E4.3 --aspect contract
  EXPECT: LEAF E4.3 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: aarch64 cell (cross + QEMU): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E4.3 --aspect evidence
  EXPECT: LEAF E4.3 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: aarch64 cell (cross + QEMU): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E4.3 --aspect coverage
  EXPECT: LEAF E4.3 COVERAGE PASS
  EVIDENCE: blocked: pending formal completion-verify; local cell closed.

- [ ] G4: aarch64 cell (cross + QEMU): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E4.3 --aspect regression
  EXPECT: LEAF E4.3 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: aarch64 cell (cross + QEMU): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E4.3 --aspect mutation
  EXPECT: LEAF E4.3 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: aarch64 cell (cross + QEMU): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate E4.3
  EXPECT: LEAF E4.3 PASS
  EVIDENCE: blocked: 2026-08-26 focused tests + toolchain evidence audit passed; full leaf-gate skipped per assignment.
