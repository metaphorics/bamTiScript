# Gates: E3.1 AOT object emission per target triple

Scope: E3.1 owns crates/bamts-codegen/src/aot/emission.rs; it delivers AOT object emission per target triple.

- [ ] G1: AOT object emission per target triple: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E3.1 --aspect contract
  EXPECT: LEAF E3.1 CONTRACT PASS
  EVIDENCE: blocked: 2026-08-26 in-tree target-emission contract is wired through the sole PIC AOT backend; `cargo test -p bamts-codegen --features aot --lib aot` passed 29 tests, including registered-target lookup, typed rejection, deterministic duplicate emission, digest identity, linked-image W+X rejection, and the no-absolute-relocation executable-section guard. Completion-aspect receipt remains unchecked until its dedicated verifier is run.

- [ ] G2: AOT object emission per target triple: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E3.1 --aspect evidence
  EXPECT: LEAF E3.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: AOT object emission per target triple: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E3.1 --aspect coverage
  EXPECT: LEAF E3.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: AOT object emission per target triple: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E3.1 --aspect regression
  EXPECT: LEAF E3.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: AOT object emission per target triple: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E3.1 --aspect mutation
  EXPECT: LEAF E3.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: AOT object emission per target triple: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate E3.1
  EXPECT: LEAF E3.1 PASS
  EVIDENCE: pending
