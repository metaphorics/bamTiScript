# Gates: B3.7 overloads + signature resolution parity

Scope: B3.7 owns crates/bamts-compiler/src/checker/overloads.rs; it delivers overloads + signature resolution parity.

- [ ] G1: overloads + signature resolution parity: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.7 --aspect contract
  EXPECT: LEAF B3.7 CONTRACT PASS
  EVIDENCE: blocked: isolated checker snapshot, `cargo test -p bamts-compiler --lib checker::overloads::tests`: 26 passed; prescribed completion verifier unavailable

- [ ] G2: overloads + signature resolution parity: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.7 --aspect evidence
  EXPECT: LEAF B3.7 EVIDENCE PASS
  EVIDENCE: blocked: pending; no B3.7 receipt registry is present

- [ ] G3: overloads + signature resolution parity: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.7 --aspect coverage
  EXPECT: LEAF B3.7 COVERAGE PASS
  EVIDENCE: blocked: pending; prescribed completion verifier unavailable

- [ ] G4: overloads + signature resolution parity: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.7 --aspect regression
  EXPECT: LEAF B3.7 REGRESSION PASS
  EVIDENCE: blocked: isolated full overload module suite remained 26/26 after restoring the mutation; prescribed completion verifier unavailable

- [ ] G5: overloads + signature resolution parity: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.7 --aspect mutation
  EXPECT: LEAF B3.7 MUTATION PASS
  EVIDENCE: blocked: isolated mutation reversed overload candidate traversal; full module suite failed the declaration-order contract

- [ ] G6: overloads + signature resolution parity: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B3.7
  EXPECT: LEAF B3.7 PASS
  EVIDENCE: blocked: UNMET: canonical `just leaf-gate B3.7` runner is absent. Scoped proof on 2026-08-26: all 27 overload tests ran within the 739/739 integrated checker pass, including the new exact inferred-constraint diagnostic regression; `cargo check -p bamts-compiler --lib` passed. G1-G5 remain unchecked without current completion receipts; the recorded mutation evidence above is retained.
