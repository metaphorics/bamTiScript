# Gates: C7.1 WeakRef + FinalizationRegistry (GC hooks)

Scope: C7.1 owns crates/bamts-runtime/src/builtins/weakref_finalization.rs; it delivers WeakRef + FinalizationRegistry (GC hooks).

- [ ] G1: WeakRef + FinalizationRegistry (GC hooks): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C7.1 --aspect contract
  EXPECT: LEAF C7.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: WeakRef + FinalizationRegistry (GC hooks): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C7.1 --aspect evidence
  EXPECT: LEAF C7.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: WeakRef + FinalizationRegistry (GC hooks): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C7.1 --aspect coverage
  EXPECT: LEAF C7.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: WeakRef + FinalizationRegistry (GC hooks): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C7.1 --aspect regression
  EXPECT: LEAF C7.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: WeakRef + FinalizationRegistry (GC hooks): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C7.1 --aspect mutation
  EXPECT: LEAF C7.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: WeakRef + FinalizationRegistry (GC hooks): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C7.1
  EXPECT: LEAF C7.1 PASS
  EVIDENCE: pending

## Runtime GC/data wave evidence — 2026-08-26

- crates/bamts-runtime/src/gc.rs now traces the current job's kept-alive values, queued cleanup registries, and FinalizationCleanup microtasks; it traces only the strong fields of WeakRef/FinalizationRegistry entries and processes dead weak targets after the WeakMap ephemeron fixed point.
- crates/bamts-runtime/src/builtins/weakref_finalization.rs uses the canonical outer-job clear operation from lib.rs; the duplicate leaf implementation was removed.
- cargo check -p bamts-runtime --message-format short: pass with pre-existing/concurrent warnings and no errors.
- cargo test -p bamts-runtime --lib weakref_finalization: **10 passed, 0 failed, 792 filtered** (artifact://12145). Coverage includes exact constructor contracts, one-job liveness, ephemeron retention, target non-retention, unregister, cleanup failure rescheduling, and cleanup-queue/microtask GC rooting.
- The formal G1–G6 checkboxes above remain pending: no completion verifier, mutation campaign, formatter, linter, or broad leaf gate ran in this concurrent wave.

## Verification 2026-08-27 (post-966 restore)

Behavior-complete: `cargo test -p bamts-runtime --lib weakref_finalization` 10/10;
full runtime lib suite 966/966. G1-G6 stay pending (no receipt in
`.outline/evidence/`; II.0.9). Checkboxes unchanged.
