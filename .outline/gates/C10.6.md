# Gates: C10.6 getCanonicalLocales/supportedValuesOf + intl402 cross-cutting

Scope: C10.6 owns crates/bamts-runtime/src/intl/canonical_locales.rs; crates/bamts-runtime/src/intl/mod.rs; crates/bamts-runtime/src/builtins/intl/mod.rs; crates/bamts-runtime/src/builtins/intl/adapter.rs; crates/bamts-runtime/src/builtins/intl/locale.rs; crates/bamts-compiler/src/checker/intrinsic_environment.rs; it delivers getCanonicalLocales/supportedValuesOf + intl402 cross-cutting.

- [ ] G1: getCanonicalLocales/supportedValuesOf + intl402 cross-cutting: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.6 --aspect contract
  EXPECT: LEAF C10.6 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: getCanonicalLocales/supportedValuesOf + intl402 cross-cutting: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.6 --aspect evidence
  EXPECT: LEAF C10.6 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: getCanonicalLocales/supportedValuesOf + intl402 cross-cutting: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.6 --aspect coverage
  EXPECT: LEAF C10.6 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: getCanonicalLocales/supportedValuesOf + intl402 cross-cutting: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.6 --aspect regression
  EXPECT: LEAF C10.6 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: getCanonicalLocales/supportedValuesOf + intl402 cross-cutting: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.6 --aspect mutation
  EXPECT: LEAF C10.6 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: getCanonicalLocales/supportedValuesOf + intl402 cross-cutting: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C10.6
  EXPECT: LEAF C10.6 PASS
  EVIDENCE: pending
