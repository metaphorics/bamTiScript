# Gates: B3.3 conditional + infer + template-literal + mapped types

Scope: B3.3 owns crates/bamts-compiler/src/checker/conditional_types.rs; it delivers conditional + infer + template-literal + mapped types.

- [ ] G1: conditional + infer + template-literal + mapped types: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.3 --aspect contract
  EXPECT: LEAF B3.3 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: conditional + infer + template-literal + mapped types: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.3 --aspect evidence
  EXPECT: LEAF B3.3 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: conditional + infer + template-literal + mapped types: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.3 --aspect coverage
  EXPECT: LEAF B3.3 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: conditional + infer + template-literal + mapped types: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.3 --aspect regression
  EXPECT: LEAF B3.3 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: conditional + infer + template-literal + mapped types: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.3 --aspect mutation
  EXPECT: LEAF B3.3 MUTATION PASS
  EVIDENCE: blocked: Scoped mutation proof on 2026-08-26: deleting per-selected-result validation made `selected_result_depth_is_checked_before_union_normalization` fail (`artifact://9211`); deleting final-union validation made `distributed_union_layer_obeys_the_structural_depth_limit` fail (`artifact://9298`). Both checks were restored and the module passed 12/12 (`artifact://9325`). The content-addressed completion mutation receipt remains pending, so G5 stays unchecked.

- [ ] G6: conditional + infer + template-literal + mapped types: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B3.3
  EXPECT: LEAF B3.3 PASS
  EVIDENCE: blocked: UNMET: the canonical `just leaf-gate B3.3` runner is absent. Scoped proof is `cargo test -p bamts-compiler --lib checker::conditional_types` 12/12 (`artifact://9325`); no substitute G6 result is claimed.

Implementation evidence:
- `conditional_types.rs` evaluates distributive conditionals, variance-aware `infer`, finite template-literal products and matching, `keyof`, and mapped types against the canonical `TypeTable`.
- Focused module tests cover naked versus wrapped distribution, independent inference, template cross-products and captures, mapped key remapping/modifiers, union-key intersections, hard expansion limits, a selected false branch that union normalization would otherwise hide, and a distributed union whose added layer alone exceeds the structural-depth limit.
- `checker.rs` registers this module. Final scoped proof on 2026-08-26: `cargo test -p bamts-compiler --lib checker::conditional_types` passed 12/12 (`artifact://9325`); both depth guards are independently mutation-sensitive. G1-G4 remain unchecked without completion receipts; G5 retains the scoped mutation proof above but remains unchecked without its receipt; G6 remains UNMET because the canonical `just leaf-gate B3.3` runner is absent.
