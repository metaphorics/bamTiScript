# Gates: E5.1 bench/compiler-rules.toml schema + measure harness (conditions record: governor/affinity/swap; INVALID_CONDITIONS on mismatch; median-of-N)

Scope: E5.1 owns bench/compiler-rules.toml; it delivers bench/compiler-rules.toml schema + measure harness (conditions record: governor/affinity/swap; INVALID_CONDITIONS on mismatch; median-of-N).

- [ ] G1: bench/compiler-rules.toml schema + measure harness (conditions record: governor/affinity/swap; INVALID_CONDITIONS on mismatch; median-of-N): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.1 --aspect contract
  EXPECT: LEAF E5.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: bench/compiler-rules.toml schema + measure harness (conditions record: governor/affinity/swap; INVALID_CONDITIONS on mismatch; median-of-N): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.1 --aspect evidence
  EXPECT: LEAF E5.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: bench/compiler-rules.toml schema + measure harness (conditions record: governor/affinity/swap; INVALID_CONDITIONS on mismatch; median-of-N): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.1 --aspect coverage
  EXPECT: LEAF E5.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: bench/compiler-rules.toml schema + measure harness (conditions record: governor/affinity/swap; INVALID_CONDITIONS on mismatch; median-of-N): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.1 --aspect regression
  EXPECT: LEAF E5.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: bench/compiler-rules.toml schema + measure harness (conditions record: governor/affinity/swap; INVALID_CONDITIONS on mismatch; median-of-N): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.1 --aspect mutation
  EXPECT: LEAF E5.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: bench/compiler-rules.toml schema + measure harness (conditions record: governor/affinity/swap; INVALID_CONDITIONS on mismatch; median-of-N): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate E5.1
  EXPECT: LEAF E5.1 PASS
  EVIDENCE: pending
