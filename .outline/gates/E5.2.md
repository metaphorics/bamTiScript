# Gates: E5.2 jit.compile-cost/payback/queue-tail-latency accepted measurements

Scope: E5.2 owns bench/jit_benchmarks.rs; it delivers jit.compile-cost/payback/queue-tail-latency accepted measurements.

- [ ] G1: jit.compile-cost/payback/queue-tail-latency accepted measurements: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.2 --aspect contract
  EXPECT: LEAF E5.2 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: jit.compile-cost/payback/queue-tail-latency accepted measurements: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.2 --aspect evidence
  EXPECT: LEAF E5.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: jit.compile-cost/payback/queue-tail-latency accepted measurements: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.2 --aspect coverage
  EXPECT: LEAF E5.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: jit.compile-cost/payback/queue-tail-latency accepted measurements: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.2 --aspect regression
  EXPECT: LEAF E5.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: jit.compile-cost/payback/queue-tail-latency accepted measurements: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf E5.2 --aspect mutation
  EXPECT: LEAF E5.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: jit.compile-cost/payback/queue-tail-latency accepted measurements: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate E5.2
  EXPECT: LEAF E5.2 PASS
  EVIDENCE: pending
