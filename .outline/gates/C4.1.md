# Gates: C4.1 String gaps (isWellFormed/toWellFormed, at, replaceAll edge, normalize forms)

Scope: C4.1 owns crates/bamts-runtime/src/builtins/string_edge.rs; it delivers String gaps (isWellFormed/toWellFormed, at, replaceAll edge, normalize forms).

- [ ] G1: String gaps (isWellFormed/toWellFormed, at, replaceAll edge, normalize forms): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.1 --aspect contract
  EXPECT: LEAF C4.1 CONTRACT PASS
  EVIDENCE: blocked: runtime-wave focused evidence recorded in the dated section below; no completion receipt was generated in this concurrent leaf pass.

- [ ] G2: String gaps (isWellFormed/toWellFormed, at, replaceAll edge, normalize forms): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.1 --aspect evidence
  EXPECT: LEAF C4.1 EVIDENCE PASS
  EVIDENCE: blocked: runtime-wave implementation/test evidence recorded in the dated section below; completion verification was not run in this concurrent leaf pass.

- [ ] G3: String gaps (isWellFormed/toWellFormed, at, replaceAll edge, normalize forms): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.1 --aspect coverage
  EXPECT: LEAF C4.1 COVERAGE PASS
  EVIDENCE: blocked: not run in this wave; deferred to the post-integration coverage/collection check described in the gate.

- [ ] G4: String gaps (isWellFormed/toWellFormed, at, replaceAll edge, normalize forms): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.1 --aspect regression
  EXPECT: LEAF C4.1 REGRESSION PASS
  EVIDENCE: blocked: not run in this wave; deferred to the post-integration regression check described in the gate.

- [ ] G5: String gaps (isWellFormed/toWellFormed, at, replaceAll edge, normalize forms): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C4.1 --aspect mutation
  EXPECT: LEAF C4.1 MUTATION PASS
  EVIDENCE: blocked: not run in this wave; deferred to the post-integration mutation check described in the gate.

- [ ] G6: String gaps (isWellFormed/toWellFormed, at, replaceAll edge, normalize forms): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C4.1
  EXPECT: LEAF C4.1 PASS
  EVIDENCE: blocked: not run in this wave; workspace build, lint, format, and final leaf gate remain the integration owner's check.

## Runtime numeric/text wave evidence (2026-08-26)

- OWNED SOURCE: `crates/bamts-runtime/src/builtins/string_edge.rs`
- FOCUSED CHECK: `cargo test -p bamts-runtime --lib builtins::string_edge::tests`
- RESULT: PASS — central registration landed by integration owner; final rerun 13 passed, 0 failed, 930 filtered (artifact://14027).
- IMPLEMENTED: raw-UTF-16 `at`/well-formed conversion/normalization, observable `replaceAll` protocol and substitutions, malformed-surrogate boundaries, and String iterator edge tests.
- REMAINING EVIDENCE: central module/install wiring and well-known `Symbol.match`/`Symbol.replace` carriers, then a nonzero focused-test run. G1-G6 remain unchecked; broad completion commands were not run.

## Verification 2026-08-27 (post-966 restore)

Behavior-complete: `cargo test -p bamts-runtime --lib string_edge` 13/13;
full runtime lib suite 966/966. G1-G6 stay pending (no receipt in
`.outline/evidence/`; II.0.9). Checkboxes unchanged.
