# Gates: B3.8 enum/namespace semantic parity (extends enum_plan.rs/namespace_plan.rs)

Scope: B3.8 owns crates/bamts-compiler/src/checker/enum_namespace.rs; it delivers enum/namespace semantic parity (extends enum_plan.rs/namespace_plan.rs).

- [ ] G1: enum/namespace semantic parity (extends enum_plan.rs/namespace_plan.rs): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.8 --aspect contract
  EXPECT: LEAF B3.8 CONTRACT PASS
  EVIDENCE: blocked: `checker/enum_namespace.rs` implements fact-driven enum member, const-enum use, merge-order, namespace value/export checks over canonical `EnumValue`; `cargo test -p bamts-compiler --lib checker::enum_namespace` passed all 15 focused contract tests on 2026-08-25.

- [ ] G2: enum/namespace semantic parity (extends enum_plan.rs/namespace_plan.rs): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.8 --aspect evidence
  EXPECT: LEAF B3.8 EVIDENCE PASS
  EVIDENCE: blocked: pending; content-addressed receipts are owned by the integrated completion gate and were not generated in this leaf recovery.

- [ ] G3: enum/namespace semantic parity (extends enum_plan.rs/namespace_plan.rs): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.8 --aspect coverage
  EXPECT: LEAF B3.8 COVERAGE PASS
  EVIDENCE: blocked: 15 passing focused tests cover duplicate/computed/heterogeneous enum members, every const-enum use class, isolated ambient const enums, legal and illegal merge kinds/order, instantiated namespaces, export visibility, and paired accepted cases.

- [ ] G4: enum/namespace semantic parity (extends enum_plan.rs/namespace_plan.rs): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.8 --aspect regression
  EXPECT: LEAF B3.8 REGRESSION PASS
  EVIDENCE: blocked: Every rejection test in `checker::enum_namespace::tests` has a passing paired accepted case guarding over-rejection; the repository completion regression aspect remains for the integrated gate.

- [ ] G5: enum/namespace semantic parity (extends enum_plan.rs/namespace_plan.rs): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.8 --aspect mutation
  EXPECT: LEAF B3.8 MUTATION PASS
  EVIDENCE: blocked: pending; no mutation receipt was fabricated or claimed.

- [ ] G6: enum/namespace semantic parity (extends enum_plan.rs/namespace_plan.rs): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B3.8
  EXPECT: LEAF B3.8 PASS
  EVIDENCE: blocked: UNMET: canonical `just leaf-gate B3.8` runner is absent. Scoped proof on 2026-08-26: enum/namespace tests passed 15/15, `cargo check -p bamts-compiler --lib` passed, and integrated checker tests passed 739/739. G1-G5 remain unchecked; no completion or mutation receipt was fabricated.
