# Gates: C10.2 Collator

Scope: C10.2 owns crates/bamts-runtime/src/intl/collator.rs; crates/bamts-runtime/src/builtins/intl/collator.rs; it delivers Collator.

- [ ] G1: Collator: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.2 --aspect contract
  EXPECT: LEAF C10.2 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: Collator: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.2 --aspect evidence
  EXPECT: LEAF C10.2 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: Collator: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.2 --aspect coverage
  EXPECT: LEAF C10.2 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: Collator: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.2 --aspect regression
  EXPECT: LEAF C10.2 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: Collator: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.2 --aspect mutation
  EXPECT: LEAF C10.2 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: Collator: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C10.2
  EXPECT: LEAF C10.2 PASS
  EVIDENCE: pending
