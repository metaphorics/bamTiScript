# Gates: C8.3 structuredClone/serialization edge

Scope: C8.3 owns crates/bamts-runtime/src/builtins/structured_clone.rs; it delivers structuredClone/serialization edge.

- [ ] G1: structuredClone/serialization edge: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C8.3 --aspect contract
  EXPECT: LEAF C8.3 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: structuredClone/serialization edge: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C8.3 --aspect evidence
  EXPECT: LEAF C8.3 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: structuredClone/serialization edge: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C8.3 --aspect coverage
  EXPECT: LEAF C8.3 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: structuredClone/serialization edge: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C8.3 --aspect regression
  EXPECT: LEAF C8.3 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: structuredClone/serialization edge: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C8.3 --aspect mutation
  EXPECT: LEAF C8.3 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: structuredClone/serialization edge: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C8.3
  EXPECT: LEAF C8.3 PASS
  EVIDENCE: pending

## Runtime GC/data wave evidence — 2026-08-26

- The dedicated crates/bamts-runtime/src/builtins/structured_clone.rs implementation exists, but it is not in the live crate graph after a central-registry overwrite.
- cargo test -p bamts-runtime --lib builtins::structured_clone::tests: exit 0 but **0 selected, 802 filtered** (artifact://12185). The broader structured_clone filter ran only two legacy object.rs tests (artifact://12176); those do not prove the dedicated cycle/transfer/error leaf.
- Current central delta: builtins/mod.rs still routes the global to object::structured_clone and has no structured_clone module declaration. The clean integration must register the dedicated module, route the global to structured_clone::structured_clone, delete the legacy object.rs::{structured_clone, clone_value} path, and require a nonzero dedicated filter.
- The formal G1–G6 checkboxes above remain pending.

## Verification 2026-08-27 (post-966 restore)

Behavior-complete: `cargo test -p bamts-runtime --lib structured_clone` 12/12
(10 `builtins::structured_clone::tests` plus two substring matches in
`intrinsics::tests` and `vm::explicit_resource::tests`). Dedicated module
`builtins::structured_clone::tests` is 10/10. Full runtime lib suite 966/966.
`mod structured_clone` is declared. G1-G6 stay pending (no receipt in
`.outline/evidence/`; II.0.9). Checkboxes unchanged.
