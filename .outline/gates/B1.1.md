# Gates: B1.1 tsc directive parser (// @flags, @filename virtual splits) feeding driver + lane

Scope: B1.1 owns crates/bamts-compiler/src/tsc_directives.rs; it delivers tsc directive parser (// @flags, @filename virtual splits) feeding driver + lane.

- [ ] G1: tsc directive parser (// @flags, @filename virtual splits) feeding driver + lane: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.1 --aspect contract
  EXPECT: LEAF B1.1 CONTRACT PASS
  EVIDENCE: blocked: pending — scoped contract proof: cargo test -p bamts-compiler --lib tsc_directives::tests (21 passed)

- [ ] G2: tsc directive parser (// @flags, @filename virtual splits) feeding driver + lane: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.1 --aspect evidence
  EXPECT: LEAF B1.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: tsc directive parser (// @flags, @filename virtual splits) feeding driver + lane: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.1 --aspect coverage
  EXPECT: LEAF B1.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: tsc directive parser (// @flags, @filename virtual splits) feeding driver + lane: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.1 --aspect regression
  EXPECT: LEAF B1.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: tsc directive parser (// @flags, @filename virtual splits) feeding driver + lane: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B1.1 --aspect mutation
  EXPECT: LEAF B1.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: tsc directive parser (// @flags, @filename virtual splits) feeding driver + lane: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B1.1
  EXPECT: LEAF B1.1 PASS
  EVIDENCE: blocked: scoped tests passed 21 and cargo check -p bamts-compiler passed; leaf-gate rerun pending
