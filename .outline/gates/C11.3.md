# Gates: C11.3 ZonedDateTime + TimeZone protocol

Scope: C11.3 owns crates/bamts-runtime/src/temporal/zoned_date_time.rs; it delivers ZonedDateTime + TimeZone protocol.

- [ ] G1: ZonedDateTime + TimeZone protocol: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.3 --aspect contract
  EXPECT: LEAF C11.3 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: ZonedDateTime + TimeZone protocol: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.3 --aspect evidence
  EXPECT: LEAF C11.3 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: ZonedDateTime + TimeZone protocol: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.3 --aspect coverage
  EXPECT: LEAF C11.3 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: ZonedDateTime + TimeZone protocol: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.3 --aspect regression
  EXPECT: LEAF C11.3 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: ZonedDateTime + TimeZone protocol: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C11.3 --aspect mutation
  EXPECT: LEAF C11.3 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: ZonedDateTime + TimeZone protocol: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C11.3
  EXPECT: LEAF C11.3 PASS
  EVIDENCE: pending
