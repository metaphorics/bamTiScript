# Gates: C10.3 NumberFormat (+ Number/Date toLocaleString integration)

Scope: C10.3 owns crates/bamts-runtime/src/intl/number_format.rs; crates/bamts-runtime/src/intl/mathematical_value.rs; crates/bamts-runtime/src/builtins/intl/number_format.rs; it delivers NumberFormat (+ Number/Date toLocaleString integration).

- [ ] G1: NumberFormat (+ Number/Date toLocaleString integration): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.3 --aspect contract
  EXPECT: LEAF C10.3 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: NumberFormat (+ Number/Date toLocaleString integration): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.3 --aspect evidence
  EXPECT: LEAF C10.3 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: NumberFormat (+ Number/Date toLocaleString integration): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.3 --aspect coverage
  EXPECT: LEAF C10.3 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: NumberFormat (+ Number/Date toLocaleString integration): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.3 --aspect regression
  EXPECT: LEAF C10.3 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: NumberFormat (+ Number/Date toLocaleString integration): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C10.3 --aspect mutation
  EXPECT: LEAF C10.3 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: NumberFormat (+ Number/Date toLocaleString integration): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C10.3
  EXPECT: LEAF C10.3 PASS
  EVIDENCE: pending
