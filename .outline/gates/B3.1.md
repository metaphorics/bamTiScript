# Gates: B3.1 generated diagnostic message catalog + ts_code layer

Scope: B3.1 owns crates/bamts-compiler/src/generated/diagnostic_messages.rs; it delivers generated diagnostic message catalog + ts_code layer.

- [ ] G1: generated diagnostic message catalog + ts_code layer: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.1 --aspect contract
  EXPECT: LEAF B3.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: generated diagnostic message catalog + ts_code layer: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.1 --aspect evidence
  EXPECT: LEAF B3.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: generated diagnostic message catalog + ts_code layer: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.1 --aspect coverage
  EXPECT: LEAF B3.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: generated diagnostic message catalog + ts_code layer: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.1 --aspect regression
  EXPECT: LEAF B3.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: generated diagnostic message catalog + ts_code layer: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B3.1 --aspect mutation
  EXPECT: LEAF B3.1 MUTATION PASS
  EVIDENCE: blocked: MANUAL MUTATION PROOF (completion receipt still pending): temporarily changing `checker/overloads.rs::NO_OVERLOAD_MATCHES` from `BAMTS-C085` to `BAMTS-C050` made `diagnostic::tests::compiler_native_diagnostic_codes_have_unique_source_owners` fail with both owners (`checker.rs::CONSTRUCTOR_TYPE_PARAMETERS`, `checker/overloads.rs::NO_OVERLOAD_MATCHES`; artifact://13672). The source was restored byte-for-byte; the same uniqueness test then passed 1/1. The registered invariant test is the mutation detector.

- [ ] G6: generated diagnostic message catalog + ts_code layer: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B3.1
  EXPECT: LEAF B3.1 PASS
  EVIDENCE: blocked: UNMET: canonical `just leaf-gate B3.1` was not run. Scoped reconciliation proof on 2026-08-26: diagnostics-parser 4/4 (artifact://13209), overloads 27/27 (artifact://13215), enum/namespace 15/15 (artifact://13219), decorators 15/15 (artifact://13221), scanner 38/38 (artifact://13227), parser 86/86 (artifact://13237), full diagnostic module 11/11 (artifact://13657), exact source-vs-REQUIRED registry 1/1 (artifact://13664), and committed map completeness/status 1/1 (artifact://13666). The 131-row map also passed JSON schema/status/code uniqueness and stale-P006 absence assertions with `jq -e`; all 110 mapped TS codes exist in the generated catalog. Full lint, format, and workspace gates were intentionally not run.

## Diagnostic ownership reconciliation

- Checker satellite bands moved without aliases: overloads `C050-C055` -> `C085-C090`; enum/namespace `C060-C068` -> `C091-C099`; decorators `C070-C079` -> `C100-C109`.
- Binder ownership landed independently in `binder/merging.rs` and `binder/exports.rs`: native-only aggregate/graph codes `C080-C084` plus authority-backed production classifications `C110-C118 -> TS2649/TS2664-TS2671`. Binder confirmed all nine `C110-C118` codes have production emission paths.
- Scanner is the sole `BAMTS-L004` definition owner; parser imports `scanner::UNTERMINATED_REGEX`.
- The parser projection guard keeps parser `C051 -> TS1031` message-gated while the overload argument-count diagnostic remains native as `C086`.
- `diagnostic::tests::compiler_native_diagnostic_codes_have_unique_source_owners` scans compiler Rust const definitions and rejects duplicate native-code owners. `diagnostic::tests::colliding_native_diagnostic_codes_keep_their_pinned_owners` table-pins the canonical and relocated owners.
- `verification/diagnostic-code-map.json` is unique and schema-valid at 131 rows, exactly matching all compiler `BAMTS-L/P/C` const definitions with no allowlist. Authority-backed `C058-C069` rows map `C058->TS2784`, `C059->TS2351`, `C060->TS2588`, `C061->TS2540`, `C062->TS2533`, `C064->TS2536`, `C065->TS2488`, `C066->TS2511`, `C067->TS2353`, and `C068->TS2377`; aggregate accessibility `C063` and cancellation `C069` remain evidence-backed native-only/unmapped. Producerless `P006` was removed. Authority-backed rows also map `C085-C099`, `C103-C108`, and `C110-C118`; `C080-C084`, `C100-C102`, and `C109` remain explicitly native-only/unmapped because no single generated TypeScript error template has equivalent semantics.
- `facets::tests::required_registry_exactly_matches_compiler_source_definitions` enforces both directions: every native compiler const has one REQUIRED row and every REQUIRED row has a source owner. The compiler-side uniqueness test independently rejects duplicate const owners.
