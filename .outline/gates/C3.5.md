# Gates: C3.5 Atomics + SharedArrayBuffer (+ agent integration with C0.1)

Scope: C3.5 owns crates/bamts-runtime/src/builtins/atomics.rs; it delivers Atomics + SharedArrayBuffer (+ agent integration with C0.1).

- [ ] G1: Atomics + SharedArrayBuffer (+ agent integration with C0.1): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.5 --aspect contract
  EXPECT: LEAF C3.5 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: Atomics + SharedArrayBuffer (+ agent integration with C0.1): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.5 --aspect evidence
  EXPECT: LEAF C3.5 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: Atomics + SharedArrayBuffer (+ agent integration with C0.1): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.5 --aspect coverage
  EXPECT: LEAF C3.5 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: Atomics + SharedArrayBuffer (+ agent integration with C0.1): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.5 --aspect regression
  EXPECT: LEAF C3.5 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: Atomics + SharedArrayBuffer (+ agent integration with C0.1): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C3.5 --aspect mutation
  EXPECT: LEAF C3.5 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: Atomics + SharedArrayBuffer (+ agent integration with C0.1): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C3.5
  EXPECT: LEAF C3.5 PASS
  EVIDENCE: pending

## 2026-08-26 runtime-collections leaf wave

- SOURCE: `atomics.rs` now implements SharedArrayBuffer growth validation, integer/BigInt atomic families, conversion/revalidation order, modular results, deterministic immediate waitAsync outcomes, FIFO notify accounting, isLockFree, and removes wall-clock/thread fallback behavior.
- FOCUSED CHECK: `cargo test -p bamts-runtime builtins::atomics::tests -- --nocapture`.
- OBSERVED: **not verified**. A clean central interval selected zero tests because the leaf is orphaned; the latest run exited 101 in unrelated `builtins/annex_b.rs:49` (`artifact://12083`). Pending wait/waitAsync suspension also remains blocked on routing the existing deterministic Test262 agent runtime into Machine/Host waiter jobs.
- WIRING: requires module/installer registration, replacing the dummy Atomics global, SharedArrayBuffer caches/carrier/GC-accounting arms, and the deterministic host-agent waiter/timeout seam.

## Verification 2026-08-27 (post-966 restore)

STILL BLOCKED. Do not mark complete. `cargo test -p bamts-runtime --lib atomics`
is 4/4 stub tests in `builtins::tests` (`atomics_claims_no_methods` and related
namespace-tag checks). `atomics.rs` is an orphan (`mod.rs` has no `mod atomics;`);
its `#[test]` functions never compile. `builtins::atomics::tests` selects 0 tests.
`HeapEntry::SharedArrayBuffer` exists for the dummy global, but the Atomics
implementation is not wired. Matches progress.md C3.5 blocked. Checkboxes unchanged.
