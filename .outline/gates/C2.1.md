# Gates: C2.1 Proxy: all 13 traps + invariants + revocation

Scope: C2.1 owns crates/bamts-runtime/src/builtins/proxy.rs; it delivers Proxy: all 13 traps + invariants + revocation.

- [ ] G1: Proxy: all 13 traps + invariants + revocation: the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C2.1 --aspect contract
  EXPECT: LEAF C2.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: Proxy: all 13 traps + invariants + revocation: every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C2.1 --aspect evidence
  EXPECT: LEAF C2.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: Proxy: all 13 traps + invariants + revocation: the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C2.1 --aspect coverage
  EXPECT: LEAF C2.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: Proxy: all 13 traps + invariants + revocation: no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C2.1 --aspect regression
  EXPECT: LEAF C2.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: Proxy: all 13 traps + invariants + revocation: the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C2.1 --aspect mutation
  EXPECT: LEAF C2.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: Proxy: all 13 traps + invariants + revocation: the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C2.1
  EXPECT: LEAF C2.1 PASS
  EVIDENCE: pending

## Verification 2026-08-27 (post-966 restore)

NOT behavior-complete. `cargo test -p bamts-runtime --lib proxy` runs 0 tests:
`proxy.rs` is an orphan file — `builtins/mod.rs` declares no `mod proxy;`, and the
`HeapEntry` enum (lib.rs:1113) has no `Proxy`/`ProxyRevoker` variants. The
`HeapEntry::Proxy`/`ProxyRevoker` occurrences exist only inside the uncompiled
orphan file. Leaf remains blocked per progress.md; all gates stay pending.
