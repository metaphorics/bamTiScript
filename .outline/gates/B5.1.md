# Gates: B5.1 module resolution modes (node16/nodenext/bundler, resolution-mode, package exports/imports)

Scope: B5.1 owns crates/bamts-compiler/src/project/resolution.rs; it delivers module resolution modes (node16/nodenext/bundler, resolution-mode, package exports/imports).

- [ ] G1: module resolution modes (node16/nodenext/bundler, resolution-mode, package exports/imports): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.1 --aspect contract
  EXPECT: LEAF B5.1 CONTRACT PASS
  EVIDENCE: pending

- [ ] G2: module resolution modes (node16/nodenext/bundler, resolution-mode, package exports/imports): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.1 --aspect evidence
  EXPECT: LEAF B5.1 EVIDENCE PASS
  EVIDENCE: pending

- [ ] G3: module resolution modes (node16/nodenext/bundler, resolution-mode, package exports/imports): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.1 --aspect coverage
  EXPECT: LEAF B5.1 COVERAGE PASS
  EVIDENCE: pending

- [ ] G4: module resolution modes (node16/nodenext/bundler, resolution-mode, package exports/imports): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.1 --aspect regression
  EXPECT: LEAF B5.1 REGRESSION PASS
  EVIDENCE: pending

- [ ] G5: module resolution modes (node16/nodenext/bundler, resolution-mode, package exports/imports): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf B5.1 --aspect mutation
  EXPECT: LEAF B5.1 MUTATION PASS
  EVIDENCE: pending

- [ ] G6: module resolution modes (node16/nodenext/bundler, resolution-mode, package exports/imports): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate B5.1
  EXPECT: LEAF B5.1 PASS
  EVIDENCE: blocked: partial 2026-08-26 — cargo check -p bamts-compiler --lib PASS; cargo test -p bamts-compiler --lib project:: PASS (104 passed, 0 failed). Leaf lint/format and receipt aspects remain for the root gate.
