# Gates: C1.1 BigInt full family (value model, operations, literals, asIntN/asUintN, JSON interplay)

Scope: C1.1 owns crates/bamts-runtime/src/builtins/bigint.rs; it delivers BigInt full family (value model, operations, literals, asIntN/asUintN, JSON interplay).

- [ ] G1: BigInt full family (value model, operations, literals, asIntN/asUintN, JSON interplay): the declared public contract vectors pass
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C1.1 --aspect contract
  EXPECT: LEAF C1.1 CONTRACT PASS
  EVIDENCE: blocked: runtime-wave focused evidence recorded in the dated section below; no completion receipt was generated in this concurrent leaf pass.

- [ ] G2: BigInt full family (value model, operations, literals, asIntN/asUintN, JSON interplay): every selected logical obligation has a current content-addressed receipt
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C1.1 --aspect evidence
  EXPECT: LEAF C1.1 EVIDENCE PASS
  EVIDENCE: blocked: runtime-wave implementation/test evidence recorded in the dated section below; completion verification was not run in this concurrent leaf pass.

- [ ] G3: BigInt full family (value model, operations, literals, asIntN/asUintN, JSON interplay): the scoped obligation set has zero blocking, external-blocked, catalog-error, timeout, crash, skip, or missing-receipt states
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C1.1 --aspect coverage
  EXPECT: LEAF C1.1 COVERAGE PASS
  EVIDENCE: blocked: not run in this wave; deferred to the post-integration coverage/collection check described in the gate.

- [ ] G4: BigInt full family (value model, operations, literals, asIntN/asUintN, JSON interplay): no previously passing obligation regressed
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C1.1 --aspect regression
  EXPECT: LEAF C1.1 REGRESSION PASS
  EVIDENCE: blocked: not run in this wave; deferred to the post-integration regression check described in the gate.

- [ ] G5: BigInt full family (value model, operations, literals, asIntN/asUintN, JSON interplay): the registered mutation makes its contract gate fail
  CHECK: cargo run -p bamts-verification -- completion verify --leaf C1.1 --aspect mutation
  EXPECT: LEAF C1.1 MUTATION PASS
  EVIDENCE: blocked: not run in this wave; deferred to the post-integration mutation check described in the gate.

- [ ] G6: BigInt full family (value model, operations, literals, asIntN/asUintN, JSON interplay): the owned code passes its narrow repository build, lint, format, and behavioral checks
  CHECK: just leaf-gate C1.1
  EXPECT: LEAF C1.1 PASS
  EVIDENCE: blocked: not run in this wave; workspace build, lint, format, and final leaf gate remain the integration owner's check.

## Runtime central integration evidence (2026-08-26)

- OWNED SOURCE: `crates/bamts-runtime/src/{lib.rs,intrinsics.rs,builtins/mod.rs,builtins/bigint.rs,builtins/number.rs}`
- INTEGRATION: BigInt installs immediately after Symbol; its constructor and prototype are cached intrinsic roots. Primitive property lookup and boxing use the cached BigInt prototype even after the global binding is removed and garbage collection runs.
- OPERATOR CUTOVER: VM unary, arithmetic, bitwise, shift, equality, and relational paths dispatch to the arbitrary-precision `BigIntValue` helpers. The bounded `bigint_i128` and `bigint_binary` paths were deleted. Mixed arithmetic remains a `TypeError`; mixed comparison stays numeric; unsigned right shift is rejected; division by zero and negative exponents remain `RangeError`s.
- COERCION CUTOVER: `BigInt`, `BigInt.asIntN`, and `BigInt.asUintN` use one observable default-hint `ToPrimitive` conversion for object inputs, including boxed BigInts, and propagate user `valueOf` errors. Abstract equality applies the general object/primitive coercion rule before BigInt comparison, so boxed/custom BigInt objects compare correctly with Boolean, String, Number, and BigInt primitives in either operand order while preserving observable coercion and abrupt completion. `Number(BigInt)` uses the explicit arbitrary-precision-to-`f64` conversion while implicit `ToNumber(BigInt)` remains a `TypeError`.
- COMPARISON CUTOVER: BigInt/string relational comparison uses `StringToBigInt`; invalid decimal/exponent strings produce unordered comparisons rather than passing through `ToNumber`. BigInt/Number comparison retains exact arbitrary-precision versus binary64 ordering. JSON implementation ownership stayed in `json_edge`.
- FOCUSED CHECK: `cargo test -p bamts-runtime --lib bigint`
- RESULT: PASS — 37 passed, 0 failed, 816 filtered (artifact://13161).
- FOCUSED CHECK: `cargo test -p bamts-runtime --lib number`
- RESULT: PASS — 18 passed, 0 failed, 834 filtered (artifact://13123).
- FOCUSED CHECK: `cargo test -p bamts-runtime --lib json`
- RESULT: PASS — 23 passed, 0 failed, 829 filtered (artifact://13125).
- BUILD CHECK: `cargo check -p bamts-runtime --lib`
- RESULT: PASS with 37 existing/concurrent dead-code or unused warnings and no errors (artifact://13163).
- COVERAGE: nonzero tests exercise values beyond 128 bits, arithmetic, large and signed shifts, wrapper/prototype lookup, constructor/prototype liveness after global removal plus GC, explicit and implicit Number conversion, mixed equality/ordering, mixed arithmetic rejection, unsigned-shift rejection, division by zero, negative exponent, JSON rejection, boxed BigInt coercion, exactly-once observable `valueOf` for constructor/width converters/equality, propagated coercion failures, boxed and custom BigInt objects against Boolean/String in both operand orders, `StringToBigInt` relational parsing, unordered invalid string comparisons, and exact mixed Number ordering.
- REMAINING EVIDENCE: G1-G6 stay unchecked because completion receipts, coverage/regression/mutation aspects, lint/format, and `just leaf-gate C1.1` were not run in this central concurrent stage.
