# bamti-cli-win32-x64

Windows x64 artifact package for `bamti-cli`.

## Pre-release status

This is a packaging template, not a published compiler. The Rust `bamts` binary
does not exist yet, so no executable is checked into this repository. The npm
build script will place `bin/bamts.exe` here only after Cargo builds a real binary.
