# bamti-cli-darwin-x64

macOS x64 artifact package for `bamti-cli`.

## Pre-release status

This is a packaging template, not a published compiler. The `bamts` binary builds
from `crates/bamts-cli`, but no executable ships in this package yet. The npm
build script places `bin/bamts` here after Cargo builds the release binary.
