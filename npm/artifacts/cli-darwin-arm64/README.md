# @bamti/cli-darwin-arm64

macOS arm64 artifact package for `bamti-cli`.

## Pre-release status

This is a packaging template, not a published compiler. The Rust `bamts` binary
does not exist yet, so no executable is checked into this repository. The npm
build script will place `bin/bamts` here only after Cargo builds a real binary.
